# Lightweight regression fuzzing: on each PR, run native `go test -fuzz` briefly
# on the fuzz targets in the packages the PR changed, seeded from committed
# testdata/fuzz corpus. Deliberately NOT ClusterFuzzLite/libFuzzer — keyorix is
# pure-Go (memory-safe, CGO-free), so the libFuzzer+sanitizer path adds cost
# without benefit here. See claude/2026-09-13-fuzzing-tooling-decisions.md.
# Deep/continuous fuzzing lives on the dedicated rigs, not in CI.
#
# Churn gate: a *.go change fuzzes the changed packages' targets; a go.mod/go.sum
# change (a dependency bump) fuzzes EVERY fuzz-target package — a dep bump changes
# third-party parser behavior without touching any *.go in the target's package,
# and those parsers are exactly what the targets exist to catch.
name: fuzz-changed
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
env:
  FUZZTIME: 90s   # per fuzz target
jobs:
  fuzz-changed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Determine changed packages that have fuzz targets
        id: pkgs
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          base="${BASE_SHA:-$(git rev-parse HEAD~1)}"

          # A dependency bump (go.mod/go.sum) changes third-party parser behavior
          # without touching any *.go in the target's own package — exactly the
          # churn these targets exist to catch. So on a dep change, fuzz every
          # package that declares a fuzz target, not just the changed .go dirs.
          dep_changed=0
          if git diff --name-only "$base"...HEAD | grep -qE '^(go\.mod|go\.sum)$'; then
            dep_changed=1
            echo "go.mod/go.sum changed — dependency churn: fuzzing ALL fuzz-target packages."
          fi

          if [ "$dep_changed" = 1 ]; then
            changed_dirs="$(git ls-files -- '*_test.go' | xargs -r grep -lE '^func Fuzz' \
              | xargs -r -n1 dirname | sort -u)"
          else
            changed_dirs="$(git diff --name-only "$base"...HEAD -- '*.go' \
              | xargs -r -n1 dirname | sort -u)"
          fi

          pkgs=""
          while IFS= read -r d; do
            [ -n "$d" ] || continue
            d="${d#./}"
            if go test -list '^Fuzz' "./$d" 2>/dev/null | grep -q '^Fuzz'; then
              pkgs="$pkgs ./$d"
            fi
          done < <(printf '%s\n' "$changed_dirs")
          pkgs="$(echo "$pkgs" | xargs || true)"
          echo "pkgs=$pkgs" >> "$GITHUB_OUTPUT"
          if [ -z "$pkgs" ]; then
            echo "No changed packages with fuzz targets — nothing to fuzz."
          else
            echo "Will fuzz: $pkgs"
          fi
      - name: Restore fuzz corpus cache
        if: steps.pkgs.outputs.pkgs != ''
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/go-build/fuzz
          key: fuzz-corpus-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            fuzz-corpus-${{ runner.os }}-
      - name: Fuzz changed packages
        if: steps.pkgs.outputs.pkgs != ''
        env:
          PKGS: ${{ steps.pkgs.outputs.pkgs }}
        run: |
          set -euo pipefail
          for pkg in $PKGS; do
            while IFS= read -r fn; do
              echo "::group::fuzz $fn ($pkg) for $FUZZTIME"
              go test "$pkg" -run='^$' -fuzz="^${fn}$" -fuzztime="$FUZZTIME"
              echo "::endgroup::"
            done < <(go test -list '^Fuzz' "$pkg" | grep '^Fuzz')
          done
      - name: Upload crash reproducers
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: fuzz-crashers
          path: '**/testdata/fuzz/**'
          if-no-files-found: ignore
