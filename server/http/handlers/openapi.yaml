openapi: 3.0.3
info:
  title: Keyorix API
  version: 1.0.0
  description: |
    Keyorix is a self-hostable secrets manager with scoped RBAC, audit logging,
    rotation, sharing, and machine-identity authentication.

    ## Authentication
    Most endpoints require a Bearer token: `Authorization: Bearer <token>`.
    Three credential types are accepted, all via the same header:
      - **Session tokens** — issued by `POST /auth/login` (and `/auth/refresh`).
      - **Personal access tokens** (`kx_pat_…`) — created at `POST /api/v1/auth/tokens`.
      - **Machine tokens** (`kx_machine_…`, ADR-030) — issued per machine identity.
    Public endpoints (login, refresh, logout, password-reset, system init, the
    setup-token flow, health, and Prometheus metrics) need no token.

    ## Response envelope
    Unless noted, success responses wrap the payload as
    `{ "success": true, "data": <…>, "message": "<…>" }`.
    Errors use `{ "error": { "code": <int>, "message": <string>, "type": <string> } }`.

    ## Authorization
    Operations are gated by scoped RBAC permissions (e.g. `secrets.read`,
    `roles.assign`) at a project/environment scope. A 403 indicates the caller's
    roles do not grant the permission at the target scope.
servers:
  - url: /
    description: Same-origin (paths are absolute from the host root)
  - url: http://localhost:8080
    description: Local development
tags:
  - {name: Auth, description: Authentication & self-service account management}
  - {name: System, description: "Health, info, and metrics"}
  - {name: Projects, description: Projects and cross-environment drift}
  - {name: Environments, description: Project environments}
  - {name: Members, description: Project membership (role grants)}
  - {name: Memberships, description: Membership onboarding lifecycle (ADR-022)}
  - {name: Invitations, description: Invitations (ADR-024)}
  - {name: AccessRequests, description: Self-service access requests (ADR-024)}
  - {name: MachineIdentities, description: "Machine identities, tokens & RBAC (ADR-023/030)"}
  - {name: Secrets, description: "Secret lifecycle, versions, usage, risk"}
  - {name: Shares, description: Secret sharing}
  - {name: RotationPolicies, description: Rotation policies & status}
  - {name: DynamicSecrets, description: On-demand database/cloud credentials (ADR-035)}
  - {name: Users, description: User management & impersonation}
  - {name: Groups, description: Groups}
  - {name: Roles, description: Roles & permissions}
  - {name: Permissions, description: Permission catalog}
  - {name: UserRoles, description: Direct user-role assignment}
  - {name: Audit, description: "Audit logs, export, retention, tamper-evidence, anomalies"}
  - {name: Notifications, description: In-app notifications}
  - {name: Dashboard, description: Dashboard stats & activity}
paths:
  /auth/login:
    post:
      tags: [Auth]
      summary: Authenticate with username and password
      description: Authenticate with username and password and return a session bearer token.
      operationId: authLogin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username: {type: string}
                password: {type: string, format: password}
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      token: {type: string, description: Session bearer token}
                      expires_at: {type: string, format: date-time}
                      absolute_expires_at: {type: string, format: date-time, description: Hard ceiling past which refresh is refused}
                      user_id: {type: integer}
                      username: {type: string}
                      email: {type: string}
                      display_name: {type: string}
                      role: {type: string}
                      roles: {type: array, items: {type: string}}
                      permissions: {type: array, items: {type: string}}
                      password_change_required: {type: boolean}
                      account_state: {type: string}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '429': {$ref: '#/components/responses/Error'}
  /auth/logout:
    post:
      tags: [Auth]
      summary: Invalidate the supplied bearer token
      description: Invalidate the current session token, logging the user out immediately.
      operationId: authLogout
      responses:
        '200': {description: Logged out}
        '400': {$ref: '#/components/responses/Error'}
  /auth/refresh:
    post:
      tags: [Auth]
      summary: Issue a new session token and invalidate the old one
      description: Exchange the current session token for a fresh one, invalidating the previous token.
      operationId: authRefresh
      responses:
        '200':
          description: Token refreshed
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      token: {type: string}
                      expires_at: {type: string, format: date-time}
                      absolute_expires_at: {type: string, format: date-time}
        '401': {$ref: '#/components/responses/Error'}
  /auth/password-reset:
    post:
      tags: [Auth]
      summary: Request a password reset (enumeration-safe; always 200)
      description: Send a password-reset email to the given address; always returns 200 to prevent email enumeration.
      operationId: authPasswordReset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties: {email: {type: string, format: email}}
      responses:
        '200': {description: Always succeeds to prevent email enumeration}
  /system/init:
    post:
      tags: [System]
      summary: Bootstrap the server (admin user + default project/environments)
      description: Initialize the server with the first admin user, default project, and default environments; idempotent.
      operationId: systemInit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, email, password]
              properties:
                username: {type: string}
                email: {type: string, format: email}
                password: {type: string, format: password}
                display_name: {type: string}
                bootstrap_token: {type: string, description: 'Prefer the X-Keyorix-Bootstrap-Token header; this field is the fallback for clients that cannot set headers.'}
      responses:
        '200':
          description: System initialised (idempotent; already_initialized=true if previously bootstrapped)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      already_initialized: {type: boolean}
                      environments: {type: array, items: {type: string}}
                      project: {type: string}
                      user:
                        type: object
                        properties:
                          id: {type: integer}
                          username: {type: string}
                          email: {type: string}
        '400': {$ref: '#/components/responses/Error'}
  /auth/setup/{token}:
    get:
      tags: [Auth]
      summary: Describe a single-use setup token without consuming it
      description: Return non-sensitive metadata about a setup token (purpose, email) without consuming it.
      operationId: authGetSetupToken
      parameters:
        - {name: token, in: path, required: true, schema: {type: string}}
      responses:
        '200':
          description: Non-sensitive description of the setup token
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      purpose: {type: string}
                      email: {type: string}
                      display_name: {type: string}
        '410': {description: The setup link is no longer valid (unknown/expired/used)}
  /auth/setup/consume:
    post:
      tags: [Auth]
      summary: Consume a setup token, set the password, and log in
      description: Consume a one-time setup token to set the account password and return a session token.
      operationId: authConsumeSetup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, password]
              properties:
                token: {type: string}
                password: {type: string, format: password}
      responses:
        '200': {description: 'Account setup complete; payload identical to login'}
        '400': {$ref: '#/components/responses/Error'}
  /health:
    get:
      tags: [System]
      summary: Liveness/health check (not wrapped in the data/message envelope)
      description: Return server health status including version and subsystem check results.
      operationId: healthCheck
      responses:
        '200':
          description: Health status
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: {type: string, example: healthy}
                  timestamp: {type: string, format: date-time}
                  version: {type: string}
                  checks: {type: object}
  /api/v1/version:
    get:
      tags: [System]
      summary: Version-skew check (unauthenticated)
      description: >-
        Return the server's REST API version and the minimum CLI version it accepts (ADR-108
        PR 0). Unauthenticated, unlike /api/v1/system/info -- a thin CLI must be able to check
        compatibility before it has credentials. Deliberately narrow: never returns the build
        version or commit (see /health's own description for the same disclosure rationale).
      operationId: getVersion
      responses:
        '200':
          description: Version-skew info
          content:
            application/json:
              schema:
                type: object
                properties:
                  api_version: {type: integer, example: 1}
                  minimum_cli_version: {type: string, example: '1.0.0'}
  /metrics:
    get:
      tags: [System]
      summary: Prometheus metrics exposition
      description: Expose runtime metrics in Prometheus text exposition format for scraping.
      operationId: prometheusMetrics
      responses:
        '200':
          description: Prometheus text exposition format
          content:
            text/plain: {schema: {type: string}}
  /api/v1/auth/profile:
    get:
      tags: [Auth]
      summary: Get the current user's profile
      description: Return the authenticated user's profile including roles, permissions, and last login.
      operationId: getAuthProfile
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Self profile (id, username, email, display_name, role, roles, permissions, last_login_at)"}
        '401': {$ref: '#/components/responses/Error'}
    put:
      tags: [Auth]
      summary: Update own display name and email
      description: Update the authenticated user's own display name and email address.
      operationId: updateAuthProfile
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name: {type: string}
                email: {type: string, format: email}
      responses:
        '200': {description: Profile updated}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/auth/change-password:
    post:
      tags: [Auth]
      summary: Change own password (drops other sessions)
      description: Change the authenticated user's password; invalidates all other active sessions.
      operationId: changePassword
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, new_password]
              properties:
                current_password: {type: string, format: password}
                new_password: {type: string, format: password}
      responses:
        '200': {description: Password changed}
        '400': {$ref: '#/components/responses/Error'}
  /api/v1/auth/sessions:
    get:
      tags: [Auth]
      summary: List the caller's active sessions
      description: Return all active sessions for the authenticated user, with the current session flagged.
      operationId: listSessions
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Active sessions; the request own session is flagged current'}
  /api/v1/auth/sessions/{id}:
    delete:
      tags: [Auth]
      summary: Revoke one of the caller's sessions
      description: Revoke a specific active session by ID, immediately invalidating its token.
      operationId: revokeSession
      security: [{bearerAuth: []}]
      parameters: [{name: id, in: path, required: true, schema: {type: integer}}]
      responses:
        '204': {description: Session revoked}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/auth/tokens:
    get:
      tags: [Auth]
      summary: List the caller's personal access tokens
      description: Return all personal access tokens belonging to the authenticated user (secret never returned).
      operationId: listPATs
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope with `data` (array of personal access tokens; secret never returned).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: array, items: {$ref: '#/components/schemas/PATToken'}}
    post:
      tags: [Auth]
      summary: Create a personal access token (raw token returned once)
      description: Create a personal access token with optional scopes and expiry; the raw token is returned only once.
      operationId: createPAT
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                expires_at: {type: string, format: date-time, nullable: true, description: 'RFC3339; omit/null for non-expiring'}
                scopes: {type: array, items: {type: string}, description: 'ADR-042 least-privilege allowlist (e.g. ["secrets.read"], supports "*" and "secrets.*"). Omit/empty = inherit the owner''s full permissions. Only ever narrows below the owner.'}
                project_scope: {type: integer, description: 'ADR-042: confine the token to a single project id; 0/omit = any project the owner can reach.'}
                environment_scope: {type: integer, description: 'ADR-042: confine the token to a single environment id; 0/omit = any environment. Env ids are globally unique, so this also pins the project.'}
                allowed_cidrs: {type: array, items: {type: string}, description: 'Restrict the token to source IPs in these CIDRs. Omit/empty = no network restriction.'}
      responses:
        '201':
          description: Envelope with `data` (`token`, the plaintext secret shown once, and `pat`, its metadata).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      token: {type: string, description: 'Plaintext token, shown only in this response'}
                      pat: {$ref: '#/components/schemas/PATToken'}
        '400': {$ref: '#/components/responses/Error'}
  /api/v1/auth/tokens/expired:
    get:
      tags: [Auth]
      summary: List the caller's own expired (non-revoked) personal access tokens
      description: Self-service; lists expired PATs belonging to the authenticated user so they can be reviewed before cleanup.
      operationId: listExpiredPATs
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope with `data` (array of personal access tokens).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: array, items: {$ref: '#/components/schemas/PATToken'}}
    delete:
      tags: [Auth]
      summary: Bulk-revoke all of the caller's expired personal access tokens
      description: Self-service; revokes every expired (non-revoked) PAT belonging to the authenticated user in one call.
      operationId: bulkRevokeExpiredPATs
      security: [{bearerAuth: []}]
      responses:
        '204': {description: All expired tokens revoked.}
  /api/v1/pat-hygiene:
    get:
      tags: [Auth]
      summary: List stale or expired-but-active personal access tokens across all users (admin)
      description: >-
        Deployment-wide personal access tokens that should probably be revoked: expired-but-never-revoked,
        and unused-for-a-long-window (token sprawl). Requires global audit.read. Never returns a token secret.
      operationId: patHygiene
      security: [{bearerAuth: []}]
      parameters:
        - {name: days, in: query, schema: {type: integer}, description: "Staleness window in days (default server-side: 90, cap 3650)."}
      responses:
        '200':
          description: Envelope with `data.tokens` (array of PAT hygiene rows).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tokens: {type: array, items: {$ref: '#/components/schemas/PATHygieneRow'}}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/auth/tokens/{id}:
    delete:
      tags: [Auth]
      summary: Revoke one of the caller's personal access tokens
      description: Permanently revoke a personal access token by ID, preventing further use.
      operationId: revokePAT
      security: [{bearerAuth: []}]
      parameters: [{name: id, in: path, required: true, schema: {type: integer}}]
      responses:
        '204': {description: Token revoked}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/auth/mfa/stepup:
    post:
      tags: [Auth]
      summary: Re-verify TOTP/recovery code to open a restricted-secret read window
      description: >-
        Re-verify the caller's TOTP (or a recovery code) without a full re-login. On
        success, opens a 15-minute server-side window permitting reads of "restricted"
        classified secrets. Blocked under impersonation.
      operationId: mfaStepUp
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code: {type: string, description: TOTP code or a recovery code}
      responses:
        '200': {description: Step-up verified; the 15-minute restricted-read window is open}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/auth/end-impersonation:
    post:
      tags: [Auth]
      summary: End the current impersonation session
      description: Terminate the active impersonation session and return to the original user context.
      operationId: endImpersonation
      security: [{bearerAuth: []}]
      responses:
        '200': {description: Impersonation ended}
  /api/v1/system/info:
    get:
      tags: [System]
      summary: Server information (requires system.read)
      description: Return server version, build info, database status, and enabled security features.
      operationId: getSystemInfo
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'System info (version, build, db, security features)'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/system/metrics:
    get:
      tags: [System]
      summary: System performance metrics (requires system.read)
      description: Return runtime, HTTP, database, and secrets performance metrics for the deployment.
      operationId: getSystemMetrics
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Runtime, HTTP, database, and secrets metrics'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/system/auth-config:
    get:
      tags: [System]
      summary: Authentication configuration (requires system.read)
      description: Return session, password policy, login lockout, MFA, WebAuthn, and SSO provider settings (secrets redacted).
      operationId: getAuthConfig
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Authentication configuration summary'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/system/encryption-config:
    get:
      tags: [System]
      summary: Encryption configuration (requires system.read)
      description: Return whether encryption is enabled and the key-provider type (key material locations redacted).
      operationId: getEncryptionConfig
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Encryption configuration summary'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/admin/jobs/role-expiry-check:
    post:
      tags: [System]
      summary: Trigger a role-expiry notification scan (requires system.write)
      description: >-
        Send immediate role-expiry notifications for time-bound role grants approaching their
        deadline (warning within 7 days, critical within 1 day). On-demand trigger for the job
        that otherwise runs only on its background scheduler.
      operationId: runRoleExpiryCheck
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Scan complete: {warnings, criticals}'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/admin/jobs/token-expiry-check:
    post:
      tags: [System]
      summary: Trigger a PAT/machine-credential expiry notification scan (requires system.write)
      description: >-
        Send immediate expiry notifications for personal access tokens and machine-identity
        credentials approaching their deadline (warning within 7 days, critical within 1 day).
        On-demand trigger for the job that otherwise runs only on its background scheduler.
      operationId: runTokenExpiryCheck
      security: [{bearerAuth: []}]
      responses:
        '200': {description: 'Scan complete: {pat_warnings, pat_criticals, machine_warnings, machine_criticals}'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/admin/usage:
    get:
      tags: [System]
      summary: Per-project secret usage report (requires audit.read)
      description: >-
        Return a per-project usage report (secret counts and read activity) for the given
        reporting window, for every project or one named via project_id.
      operationId: getUsageReport
      security: [{bearerAuth: []}]
      parameters:
        - {name: days, in: query, description: 'Reporting window in days (1-365, default 30)', schema: {type: integer}}
        - {name: project_id, in: query, description: 'Scope to a single project (omit for all)', schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data` (the usage report).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/UsageReport'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/admin/billing/report:
    get:
      tags: [System]
      summary: Per-project FinOps billing report (requires audit.read and the billing license feature)
      description: >-
        Return a per-project billing report (secret counts, read/write activity, machine vs.
        human breakdowns) for a date range, for every project or a caller-supplied comma-separated
        project_id list.
      operationId: getBillingReport
      security: [{bearerAuth: []}]
      parameters:
        - {name: from, in: query, required: true, description: 'RFC3339 start of the billing window', schema: {type: string, format: date-time}}
        - {name: to, in: query, required: true, description: 'RFC3339 end of the billing window', schema: {type: string, format: date-time}}
        - {name: project_id, in: query, description: 'Comma-separated project IDs to include (omit for all)', schema: {type: string}}
      responses:
        '200':
          description: Envelope with `data` (the billing report).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/BillingReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/projects:
    get:
      tags: [Projects]
      summary: List projects with secret and environment counts
      description: List all projects accessible to the caller, including secret and environment counts.
      operationId: listProjects
      security: [{bearerAuth: []}]
      parameters:
        - name: include_deleted
          in: query
          description: When 'true', also returns soft-deleted projects (each flagged via deleted/deleted_at) for the restore UI.
          schema: {type: string, enum: ['true', 'false']}
      responses:
        '200':
          description: "Envelope {data, message}. data.projects is an array of project objects (including secret and environment counts; soft-deleted ones carry deleted/deleted_at)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      projects:
                        type: array
                        items:
                          type: object
                          properties:
                            id: {type: integer}
                            name: {type: string}
                            description: {type: string}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Projects]
      summary: Create a project
      description: Create a new project with the given name and optional description, optionally seeding a specific set of environment names.
      operationId: createProject
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                description: {type: string}
                environments:
                  type: array
                  description: "Environment names to seed (default, if omitted: development, staging, production)."
                  items: {type: string}
      responses:
        '201':
          description: "Envelope {data, message='Project created'}. data is the created project object."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Project'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}:
    get:
      tags: [Projects]
      summary: Get a project by ID
      description: Return a single project by its numeric ID, including metadata and counts.
      operationId: getProject
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data, message}. data is the project object."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Project'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Projects]
      summary: Update a project's name/description
      description: Update the name and/or description of an existing project by ID.
      operationId: updateProject
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: {type: string}
                description: {type: string}
      responses:
        '200':
          description: "Envelope {data, message='Project updated'}. data is the updated project object."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Project'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Projects]
      summary: Delete a project
      description: Soft-delete a project by ID; use force=true to cascade-delete even when the project contains secrets.
      operationId: deleteProject
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: force
          in: query
          description: When 'true', cascade-delete even when the project contains secrets (otherwise a non-empty project yields 409).
          schema: {type: string, enum: ['true', 'false']}
      responses:
        '200':
          description: "Envelope {data=null, message='Project deleted'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/restore:
    post:
      tags: [Projects]
      summary: Restore a soft-deleted project
      description: Restore a previously soft-deleted project, making it active again.
      operationId: restoreProject
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data=null, message='Project restored'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/drift:
    get:
      tags: [Projects]
      summary: Cross-environment drift report for a project
      description: Return a cross-environment drift report showing secret keys that are missing or diverge across environments.
      operationId: getProjectDrift
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data, message}. data is the drift report: environments[] (per-environment listing), drifted_keys[] (keys missing in some environments or with diverging settings), and summary."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/stats:
    get:
      tags: [Projects]
      summary: Rich per-project statistics (ADR-108 PR 6 addition)
      description: >-
        Secret counts, rotation health, unique accessors, open anomalies, and a
        classification breakdown for a project. Added for ADR-108 PR 6 (thin CLI
        `project stats`) -- was already a live route (secretHandler.GetProjectStats,
        router.go), just undocumented in this spec until now.
      operationId: getProjectStats
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: "Envelope {data, message}. data is a per-project stats object (project_id, project_name, total/active/expired/expiring_in_30_days secrets, rotation_enabled, overdue_rotation, last_rotation_at, unique_accessors, open_anomalies, classification_counts, computed_at)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/hygiene:
    get:
      tags: [Projects]
      summary: A project's security-hygiene posture, counts only (ADR-108 PR 6 addition)
      description: >-
        Outstanding cleanup signals for a project in one call: secrets whose owner
        departed, secrets unused for a long window, secrets expiring/expired, and
        stale machine identities. Added for ADR-108 PR 6 (thin CLI `project hygiene`)
        -- was already a live route (secretHandler.ProjectHygiene, router.go), just
        undocumented in this spec until now.
      operationId: getProjectHygiene
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: "Envelope {data, message}. data has orphaned_secrets, unused_secrets, expiring_secrets, stale_machine_identities, rotation_overdue (all integer counts)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/health:
    get:
      tags: [Projects]
      summary: Secret health summary for a project (ADR-108 PR 6 addition)
      description: >-
        A ranked list of the highest-risk secrets in a project together with overall
        band counts (HIGH/MEDIUM/LOW). Added for ADR-108 PR 6 (thin CLI `project
        health`) -- was already a live route (secretHandler.GetProjectHealth,
        router.go), just undocumented in this spec until now.
      operationId: getProjectHealth
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - name: limit
          in: query
          description: Maximum number of secrets to display (1-100, default 20).
          schema: {type: integer}
      responses:
        '200':
          description: "Envelope {data, message}. data has project_id, total_secrets, high_risk_count, medium_risk_count, low_risk_count, and secrets[] (secret_id, secret_name, score, band, factors[] of key/label/score/weight/detail)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/rotation-order:
    get:
      tags: [RotationPolicies]
      summary: Print a safe rotation sequence for a project's dependency graph (ADR-052)
      description: >-
        A topological sort of the project's secret dependency graph: each secret
        is listed before anything that depends on it. Added for ADR-108 PR 1
        (thin CLI `rotation order`) — this route existed and was already called
        by the old CLI's remote mode; it was simply undocumented in this spec
        until now.
      operationId: getProjectRotationOrder
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is a RotationOrder.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/RotationOrder'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/rotation-plan:
    get:
      tags: [RotationPolicies]
      summary: Print an automated rotation plan (dependency-safe waves, by urgency, ADR-053)
      description: >-
        A project's overdue/due-soon secrets batched into dependency-safe waves,
        prioritised by urgency and annotated with why. Added for ADR-108 PR 1
        (thin CLI `rotation plan`) — see getProjectRotationOrder above.
      operationId: getProjectRotationPlan
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is a RotationPlan.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/RotationPlan'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review:
    get:
      tags: [Projects]
      summary: Access recertification report (ISO 27001 A.5.18)
      description: >-
        Who can reach the project's secrets and how: role-based standing access
        (project-scoped role grants conferring a secrets.* permission) plus the
        per-secret grants (ownership and direct/group shares). Requires roles.read
        at the project scope.
      operationId: getProjectAccessReview
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: {entries[], count}; each entry is
            {principal_type (user|group), principal_id, principal_name, email,
            source (role|owner|direct_share|group_share), role_id, role_name,
            access_level, environment_id, secret_id, secret_name, last_used_at
            (the user principal's most recent secret access in the project — absent
            = no recorded activity, i.e. dormant standing access)}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/attest:
    post:
      tags: [Projects]
      summary: Attest an access-review grant (certify reviewed & kept)
      description: >-
        Record that the grant in the body was reviewed and intentionally kept —
        the audit evidence of recertification (access_review.attested). Changes no
        access. Requires roles.read at the project scope.
      operationId: attestProjectAccessReview
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AccessReviewDecision'}
      responses:
        '200': {description: "Envelope {data, message}. Attested."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/revoke:
    post:
      tags: [Projects]
      summary: Revoke an access-review grant (remove the role or share)
      description: >-
        Close the recertification loop by removing the grant in the body — a
        project-scoped role assignment (source=role, role_id set) or a secret share
        (source=direct_share|group_share, secret_id set). Audited as
        access_review.revoked. Ownership (source=owner) cannot be revoked here.
        Requires roles.assign at the project scope.
      operationId: revokeProjectAccessReview
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/AccessReviewDecision'}
      responses:
        '200': {description: "Envelope {data, message}. Access revoked."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/campaigns:
    get:
      tags: [Projects]
      summary: List access-review campaigns (periodic recertification, A.5.18)
      description: >-
        The project's recertification campaigns, newest first, each with its
        decision tally {total, pending, attested, revoked}. Requires roles.read.
      operationId: listAccessReviewCampaigns
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200': {description: "Envelope {data}. data: {campaigns[] of {campaign, progress}, count}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Projects]
      summary: Open an access-review campaign (snapshot current access)
      description: >-
        Snapshot the project's current access review into per-grant campaign items
        (each pending) for a tracked recertification cycle. Requires roles.assign.
      operationId: openAccessReviewCampaign
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name: {type: string, description: "Campaign label, e.g. \"Q4 2026 access recertification\""}
      responses:
        '201': {description: "Envelope {data}. data: {campaign, progress}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/campaigns/{campaignId}:
    get:
      tags: [Projects]
      summary: Get a campaign with its items and progress
      description: Return a single access-review campaign with all its items and recertification progress.
      operationId: getAccessReviewCampaign
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: campaignId, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200': {description: "Envelope {data}. data: {campaign, items[] (snapshot + decision), progress}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/campaigns/{campaignId}/export.csv:
    get:
      tags: [Projects]
      summary: Download campaign items as CSV (auditor evidence export)
      description: >-
        Returns every item of the campaign with its recertification decision as
        a CSV attachment. Intended as the signed-off recertification record for
        ISO 27001 A.5.18 archiving. No secret values are included. Gated by
        roles.read at project scope.
      operationId: exportAccessReviewCampaignCSV
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: campaignId, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: CSV file download (`text/csv`). Columns — id, item_type, subject, role/permission, decision, decided_by, decided_at, created_at.
          content:
            text/csv:
              schema: {type: string, format: binary}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/campaigns/{campaignId}/items/{itemId}/decide:
    post:
      tags: [Projects]
      summary: Decide one campaign item (attest or revoke)
      description: >-
        Record a recertification decision on one item of an open campaign: attest
        keeps the grant (evidence only); revoke removes the underlying grant. Both
        audited. Requires roles.assign.
      operationId: decideAccessReviewCampaignItem
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: campaignId, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: itemId, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action: {type: string, enum: [attest, revoke]}
                reason: {type: string, description: "Optional reviewer note"}
      responses:
        '200': {description: "Envelope {data, message}. Decision recorded."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/access-review/campaigns/{campaignId}/close:
    post:
      tags: [Projects]
      summary: Close a campaign (freeze as evidence)
      description: >-
        Freeze the campaign as the recertification evidence record. Refuses while
        items remain pending unless force=true. Requires roles.assign.
      operationId: closeAccessReviewCampaign
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: campaignId, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                force: {type: boolean, description: "Close even if items remain pending"}
      responses:
        '200': {description: "Envelope {data}. data: {campaign, progress}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/break-glass:
    post:
      tags: [Projects]
      summary: Activate break-glass emergency access (self-service)
      description: >-
        Immediately self-grant the configured emergency role at the project scope,
        time-bound (default + capped by server config), with a required written
        justification. NOT RBAC-gated — the controls are config-enablement, the
        justification, a loud audit (break_glass.activated) + admin alert, and
        auto-expiry. Returns 403 when break-glass is disabled.
      operationId: activateBreakGlass
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [justification]
              properties:
                justification: {type: string, description: "Written reason (required)"}
                ttl: {type: string, description: "Optional grant-lifetime override, a Go duration (e.g. 2h); capped at the server max"}
      responses:
        '201':
          description: "Envelope {data}. data: {activation}."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      activation: {$ref: '#/components/schemas/BreakGlassActivation'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    get:
      tags: [Projects]
      summary: List break-glass activations (for review)
      description: >-
        The project's emergency-access activations, newest first (an active grant
        past its expiry reads as expired). Requires roles.read.
      operationId: listBreakGlassActivations
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200':
          description: "Envelope {data}. data: {activations[] of {id, user_id, role_name, justification, state, expires_at, created_at}, count}."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      activations:
                        type: array
                        items: {$ref: '#/components/schemas/BreakGlassActivation'}
                      count: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/break-glass/{activationId}/revoke:
    post:
      tags: [Projects]
      summary: Revoke an active break-glass grant early
      description: >-
        End an active emergency grant before its TTL — removes the role assignment
        and marks the activation revoked. Requires roles.assign.
      operationId: revokeBreakGlass
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
        - {name: activationId, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200': {description: "Envelope {data, message}. Emergency access revoked."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/members:
    get:
      tags: [Members]
      summary: List a project's members (role grants)
      description: Return all members of a project with their assigned role grants.
      operationId: listProjectMembers
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data, message}. data.members is an array of project member objects."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Members]
      summary: Add a member to a project with a role
      description: Add a user to a project by granting them the specified role.
      operationId: addProjectMember
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, role]
              properties:
                user_id: {type: integer, format: uint32}
                role: {type: string}
      responses:
        '201':
          description: "Envelope {data=null, message='Member added'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/members/{userId}:
    put:
      tags: [Members]
      summary: Update a project member's role
      description: Change the role assigned to an existing project member.
      operationId: updateProjectMember
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: userId
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role: {type: string}
      responses:
        '200':
          description: "Envelope {data=null, message='Member role updated'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Members]
      summary: Remove a member from a project
      description: Remove a user from a project, revoking all their project-scoped role grants.
      operationId: removeProjectMember
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: userId
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data=null, message='Member removed'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/memberships:
    get:
      tags: [Memberships]
      summary: List project memberships (onboarding state machine)
      description: List onboarding memberships for a project, optionally filtering for stale invited records.
      operationId: listProjectMemberships
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: stale
          in: query
          description: When 'true', returns only this project's invited memberships older than 7 days.
          schema: {type: string, enum: ['true', 'false']}
      responses:
        '200':
          description: "Envelope {data, message}. data.memberships is an array of membership objects (state: invited|identity_verified|provisioned|active|revoked)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Memberships]
      summary: Invite a member (creates an invited membership)
      description: Create an invited membership for a user, starting the onboarding state machine.
      operationId: inviteMember
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, role]
              properties:
                user_id: {type: integer, format: uint32}
                role: {type: string}
                idp_resolved: {type: boolean}
      responses:
        '201':
          description: "Envelope {data, message='Member invited'}. data.membership is the created membership object."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/memberships/{membershipId}:
    put:
      tags: [Memberships]
      summary: Transition a membership through the onboarding state machine
      description: Advance or revoke a membership by applying verify, provision, activate, or revoke actions.
      operationId: transitionMembership
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: membershipId
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action:
                  type: string
                  enum: [verify, provision, activate, revoke]
                  description: "verify→identity_verified, provision→provisioned, activate→active, revoke→revoked."
      responses:
        '200':
          description: "Envelope {data, message='Membership updated'}. data.membership is the updated membership object."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/environments:
    get:
      tags: [Environments]
      summary: List all environments (global, backward-compat)
      description: Return all environments across all projects accessible to the authenticated user.
      operationId: listEnvironments
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope {data, message}. data.environments is an array of environment objects."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      environments: {type: array, items: {$ref: '#/components/schemas/Environment'}}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/environments:
    get:
      tags: [Environments]
      summary: List a project's environments
      description: Return all environments belonging to the specified project, optionally including soft-deleted ones.
      operationId: listProjectEnvironments
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
        - name: include_deleted
          in: query
          description: When 'true', also returns soft-deleted environments.
          schema: {type: string, enum: ['true', 'false']}
      responses:
        '200':
          description: "Envelope {data, message}. data.environments is an array of environment objects."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      environments: {type: array, items: {$ref: '#/components/schemas/Environment'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Environments]
      summary: Create an environment in a project
      description: Create a new named environment within the specified project.
      operationId: createProjectEnvironment
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
      responses:
        '201':
          description: "Envelope {data, message='Environment created'}. data is the created environment object (project_id, name)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Environment'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/environments/{envId}/clone:
    post:
      tags: [Environments]
      summary: Clone all secrets from one environment to another in the same project (ADR-108 PR 6 addition)
      description: >-
        Copies every secret from the source environment (path envId) into the
        destination environment (body destination_environment_id). Secrets that
        already exist by name in the destination are skipped, not overwritten. Only
        the latest version of each secret is copied. Requires secrets.write on the
        project scope (creates in destination) and secrets.read on the source
        environment scope. Added for ADR-108 PR 6 (thin CLI `project env clone`) --
        was already a live route (catalogHandler.CloneEnvironment, router.go), just
        undocumented in this spec until now.
      operationId: cloneEnvironment
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, description: Project ID, schema: {type: integer, format: uint32}}
        - {name: envId, in: path, required: true, description: Source environment ID, schema: {type: integer, format: uint32}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [destination_environment_id]
              properties:
                destination_environment_id: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data, message}. data is an EnvCloneResult."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/EnvCloneResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{projectId}/environments/{id}/restore:
    post:
      tags: [Environments]
      summary: Restore a soft-deleted environment
      description: Restore a previously soft-deleted environment within the specified project.
      operationId: restoreEnvironment
      security: [{bearerAuth: []}]
      parameters:
        - name: projectId
          in: path
          required: true
          description: Owning project ID; the permission scope resolves from it because the environment row is soft-deleted.
          schema: {type: integer, format: uint32}
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data=null, message='Environment restored'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/environments/{id}:
    delete:
      tags: [Environments]
      summary: Delete an environment
      description: Soft-delete an environment by ID; fails with 409 if the environment still contains secrets.
      operationId: deleteEnvironment
      security: [{bearerAuth: []}]
      parameters:
        - name: id
          in: path
          required: true
          schema: {type: integer, format: uint32}
      responses:
        '200':
          description: "Envelope {data=null, message='Environment deleted'}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/invitations:
    get:
      tags: [Invitations]
      summary: List project invitations
      description: Return all pending and historical invitations for the specified project.
      operationId: listProjectInvitations
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data.invitations` (array of invitation objects).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      invitations: {type: array, items: {$ref: '#/components/schemas/ProjectInvitation'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Invitations]
      summary: Invite a user to the project
      description: Send a project invitation to an email address, assigning the specified role on acceptance.
      operationId: createProjectInvitation
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, role]
              properties:
                email: {type: string, format: email}
                role: {type: string, description: Project role to assign on accept}
      responses:
        '201':
          description: >-
            Envelope with `data.invitation` plus `data.setup_link`
            (or `data.delivery_error` when the link could not be delivered).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      invitation: {$ref: '#/components/schemas/ProjectInvitation'}
                      setup_link: {$ref: '#/components/schemas/ProvisionSetupResult'}
                      delivery_error: {type: string, description: "Set instead of setup_link when the invitation was created but the link could not be delivered."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/invitations/{invitationId}:
    delete:
      tags: [Invitations]
      summary: Revoke a pending invitation
      description: Cancel a pending project invitation so it can no longer be accepted.
      operationId: revokeProjectInvitation
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: invitationId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {nullable: true}
                  message: {type: string}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/invitations/{invitationId}/resend:
    post:
      tags: [Invitations]
      summary: Resend a pending invitation link
      description: Reissue and resend the accept link for a pending project invitation.
      operationId: resendProjectInvitation
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: invitationId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data.setup_link` (the reissued accept link).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      setup_link: {$ref: '#/components/schemas/ProvisionSetupResult'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/invitations:
    post:
      tags: [Invitations]
      summary: Create a global (non-project) invitation
      description: Invite a user to the deployment globally, with optional system role and project assignments.
      operationId: createGlobalInvitation
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: {type: string, format: email}
                role:
                  type: string
                  description: System role (optional; defaults to system_viewer)
                project_assignments:
                  type: array
                  description: Per-project roles applied atomically on accept
                  items:
                    type: object
                    properties:
                      project_id: {type: integer}
                      role: {type: string}
      responses:
        '201':
          description: >-
            Envelope with `data.invitation` plus `data.setup_link`
            (or `data.delivery_error` when the link could not be delivered).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/access-requests:
    get:
      tags: [AccessRequests]
      summary: List project access requests
      description: Return all access requests for a project, including pending dual-control approval progress.
      operationId: listAccessRequests
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope with `data.access_requests` (array of access-request objects).
            Under dual control a pending request carries `ApprovalsReceived` /
            `RequiredApprovals` (the M-of-K progress).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [AccessRequests]
      summary: Request access to a project (self-service)
      description: Submit a self-service request for access to a project with an optional suggested role and reason.
      operationId: createAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                suggested_role: {type: string, description: Project role the requester would like}
                reason: {type: string}
      responses:
        '201':
          description: Envelope with `data.access_request` (the created pending request).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/access-requests/{requestId}:
    put:
      tags: [AccessRequests]
      summary: Approve or reject an access request
      description: >-
        Approve records the caller's sign-off. Under dual control
        (`dual_control.required_approvals` > 1) the role is granted only once that
        many distinct approvers — none of them the requester — have approved;
        before then the request stays pending.
      operationId: resolveAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: requestId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action: {type: string, enum: [approve, reject]}
                granted_role: {type: string, description: Role to grant on approve}
                grant_ttl: {type: string, description: "Optional on approve — time-bound (just-in-time) the granted role; a Go duration (e.g. 4h, 30m). Empty/absent = permanent. The grant stops authorizing once the TTL elapses and is swept by the JIT expiry scheduler."}
                reason: {type: string, description: Reason on reject}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/access-requests/{requestId}/withdraw:
    post:
      tags: [AccessRequests]
      summary: Withdraw your own access request (self-service)
      description: Cancel a previously submitted access request that is still pending approval.
      operationId: withdrawAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: requestId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/access-requests/bulk-approve:
    post:
      tags: [AccessRequests]
      summary: Approve multiple pending access requests at once (ADR-108 PR 7 addition)
      description: >-
        Requires a global roles.assign; the handler additionally enforces per-item
        project-scoped roles.assign for the caller before approving each request.
        Added for ADR-108 PR 7 (thin CLI `request bulk-approve`) -- was already a live
        route (catalogHandler.BulkApproveAccessRequests, router.go), just undocumented
        in this spec until now.
      operationId: bulkApproveAccessRequests
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [request_ids]
              properties:
                request_ids: {type: array, items: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data.result: {approved: [<request ids>],
            failed: [{request_id, error}]}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/access-requests/bulk-reject:
    post:
      tags: [AccessRequests]
      summary: Reject multiple pending access requests at once (ADR-108 PR 7 addition)
      description: >-
        Requires a global roles.assign; the handler additionally enforces per-item
        project-scoped roles.assign for the caller before rejecting each request.
        Added for ADR-108 PR 7 (thin CLI `request bulk-reject`) -- was already a live
        route (catalogHandler.BulkRejectAccessRequests, router.go), just undocumented
        in this spec until now.
      operationId: bulkRejectAccessRequests
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [request_ids, reason]
              properties:
                request_ids: {type: array, items: {type: integer}}
                reason: {type: string}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data.result: {rejected: [<request ids>],
            failed: [{request_id, error}]}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/rejection-reason-templates:
    get:
      tags: [AccessRequests]
      summary: List rejection-reason templates (ADR-108 PR 7 addition)
      description: >-
        Templates are not project-scoped -- requires a global roles.assign. Added for
        ADR-108 PR 7 (thin CLI `request rejection-templates list`) -- was already a
        live route (catalogHandler.ListRejectionReasonTemplates, router.go), just
        undocumented in this spec until now.
      operationId: listRejectionReasonTemplates
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope {data, message}. data.templates[]: {id, name, reason}."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [AccessRequests]
      summary: Add a rejection-reason template (ADR-108 PR 7 addition)
      description: >-
        Requires a global roles.assign. Added for ADR-108 PR 7 (thin CLI `request
        rejection-templates add`) -- was already a live route
        (catalogHandler.CreateRejectionReasonTemplate, router.go), just undocumented
        in this spec until now.
      operationId: createRejectionReasonTemplate
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, reason]
              properties:
                name: {type: string}
                reason: {type: string}
      responses:
        '201':
          description: "Envelope {data, message}. data.template: {id, name, reason}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/rejection-reason-templates/{id}:
    delete:
      tags: [AccessRequests]
      summary: Delete a rejection-reason template (ADR-108 PR 7 addition)
      description: >-
        Requires a global roles.assign. Added for ADR-108 PR 7 (thin CLI `request
        rejection-templates delete`) -- was already a live route
        (catalogHandler.DeleteRejectionReasonTemplate, router.go), just undocumented
        in this spec until now.
      operationId: deleteRejectionReasonTemplate
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: "Envelope {data=null, message='deleted'}."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/secret-access-requests:
    get:
      tags: [AccessRequests]
      summary: List your own and approvable secret access requests
      description: >-
        Returns two buckets scoped to the caller: `mine` (the caller's own
        secret-scoped access requests, any state) and `pending_approval`
        (pending requests at a project where the caller holds admin
        authority — never including the caller's own requests).
      operationId: listSecretAccessRequests
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope with `data.mine` and `data.pending_approval` (arrays of access-request objects).
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [AccessRequests]
      summary: Request approval to read one restricted secret's value (self-service)
      description: >-
        Submit a self-service request for approval to read ONE specific
        secret's value, distinct from the project/role access-request
        family. `reason` is required. A restricted secret's existence is
        never inferable from this endpoint's response: an unknown secret ID
        and one the caller cannot see both produce an identical 404.
      operationId: createSecretAccessRequest
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [secret_id, reason]
              properties:
                secret_id: {type: integer}
                reason: {type: string}
      responses:
        '201':
          description: Envelope with `data.access_request` (the created pending request).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/secret-access-requests/{requestId}:
    get:
      tags: [AccessRequests]
      summary: Get one secret access request
      description: >-
        Visible only to the request's own requester or an admin at its
        project. A nonexistent request ID, a project/role request ID (out of
        scope for this endpoint), and a real secret-scoped request the
        caller may not see all produce an identical 404.
      operationId: getSecretAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: requestId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data.access_request`.
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [AccessRequests]
      summary: Approve or reject a secret access request
      description: >-
        Unlike the project/role family's resolve endpoint, no
        granted_role/grant_ttl — a secret-scoped approval grants no role at
        all. Authority (admin at the request's own project) is enforced
        entirely inside core, since this route carries no project ID to gate
        on at the HTTP layer.
      operationId: resolveSecretAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: requestId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action: {type: string, enum: [approve, reject]}
                reason: {type: string, description: Reason on reject}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/secret-access-requests/{requestId}/withdraw:
    post:
      tags: [AccessRequests]
      summary: Withdraw your own pending secret access request (self-service)
      description: Cancel a previously submitted secret access request that is still pending approval.
      operationId: withdrawSecretAccessRequest
      security: [{bearerAuth: []}]
      parameters:
        - {name: requestId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities:
    get:
      tags: [MachineIdentities]
      summary: List machine identities
      description: Return all machine identities registered in the specified project.
      operationId: listMachineIdentities
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data.machine_identities` (array of machine-identity objects).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      machine_identities: {type: array, items: {$ref: '#/components/schemas/MachineIdentity'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [MachineIdentities]
      summary: Create a machine identity
      description: Create a new machine identity in the specified project for non-human authentication.
      operationId: createMachineIdentity
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                identity_type: {type: string}
                description: {type: string}
                classification: {type: string, description: 'Data classification: public | internal | confidential | restricted'}
      responses:
        '201':
          description: Envelope with `data.machine_identity` (the created identity).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      machine_identity: {$ref: '#/components/schemas/MachineIdentity'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/migrate-from-user:
    post:
      tags: [MachineIdentities]
      summary: Convert a user into a machine identity (ADR-023)
      description: >-
        Materialize a project machine identity for an existing user and, unless keep_user is
        set, suspend the source user so it can no longer log in. The user is never deleted.
        Requires roles.assign (scoped to this project) AND users.write (global).
      operationId: migrateUserToMachine
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username]
              properties:
                username: {type: string}
                identity_type: {type: string}
                name: {type: string}
                keep_user: {type: boolean, description: 'Leave the source user active (default: false, suspends it)'}
      responses:
        '201':
          description: Envelope with `data.machine_identity` (the created identity).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      machine_identity: {$ref: '#/components/schemas/MachineIdentity'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}:
    put:
      tags: [MachineIdentities]
      summary: Transition a machine identity state
      description: Change a machine identity's lifecycle state by applying an activate, suspend, or revoke action.
      operationId: transitionMachineIdentity
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action: {type: string, enum: [activate, suspend, revoke]}
      responses:
        '200':
          description: Envelope with `data.machine_identity` (the updated identity).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/tokens:
    get:
      tags: [MachineIdentities]
      summary: List machine tokens (metadata only)
      description: Return token metadata (prefix, expiry, last used) for a machine identity; secret values are never returned.
      operationId: listMachineTokens
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope with `data.tokens`; each item has `id`, `name`, `prefix`,
            `last_used_at`, `expires_at`, `revoked` (never the raw token).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tokens: {type: array, items: {$ref: '#/components/schemas/MachineToken'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [MachineIdentities]
      summary: Issue a machine token (raw token shown once)
      description: Issue a new authentication token for a machine identity; the raw token is returned only once.
      operationId: issueMachineToken
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                expires_in_days: {type: integer, description: Optional; omit or 0 for no expiry}
                classification: {type: string, description: 'Data classification: public | internal | confidential | restricted'}
      responses:
        '201':
          description: >-
            Envelope with `data.token` (raw token, returned ONCE), `data.id`,
            `data.prefix`, and `data.expires_at`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      token: {type: string, description: 'Plaintext token, shown only in this response'}
                      id: {type: integer}
                      prefix: {type: string}
                      expires_at: {type: string, format: date-time, nullable: true}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/tokens/{tokenId}:
    delete:
      tags: [MachineIdentities]
      summary: Revoke a machine token
      description: Permanently revoke a machine token by ID, preventing further authentication with it.
      operationId: revokeMachineToken
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
        - {name: tokenId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/roles:
    post:
      tags: [MachineIdentities]
      summary: Grant a project role to a machine identity
      description: Assign a project-scoped role to a machine identity, granting it the role's permissions.
      operationId: grantMachineRole
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role_id]
              properties:
                role_id: {type: integer}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/roles/{roleId}:
    delete:
      tags: [MachineIdentities]
      summary: Remove a project role from a machine identity
      description: Revoke a previously granted project-scoped role from a machine identity.
      operationId: removeMachineRole
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
        - {name: roleId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with null `data` and a confirmation message.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/oidc-bindings:
    get:
      tags: [MachineIdentities]
      summary: List a machine identity's OIDC federation bindings
      description: Return the OIDC (issuer, subject) bindings mapped to a machine identity (ADR-031).
      operationId: listOIDCBindings
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope with `data.bindings` (array of OIDC bindings).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      bindings: {type: array, items: {$ref: '#/components/schemas/OIDCBinding'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [MachineIdentities]
      summary: Bind an OIDC (issuer, subject) to a machine identity
      description: >-
        Create an OIDC federation binding (ADR-031) so a workload holding a matching
        external JWT can authenticate as this machine identity with no stored credential.
      operationId: createOIDCBinding
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [issuer, subject]
              properties:
                issuer: {type: string, description: "The token's iss claim"}
                subject: {type: string, description: "The token's sub claim"}
      responses:
        '201':
          description: Envelope with `data` (the created binding).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/OIDCBinding'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/machine-identities/{machineId}/oidc-bindings/{bindingId}:
    delete:
      tags: [MachineIdentities]
      summary: Remove an OIDC binding from a machine identity
      description: Permanently remove a previously created OIDC federation binding.
      operationId: deleteOIDCBinding
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: machineId, in: path, required: true, schema: {type: integer}}
        - {name: bindingId, in: path, required: true, schema: {type: integer}}
      responses:
        '200': {description: Envelope with null `data` and a confirmation message.}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/machine-token-hygiene:
    get:
      tags: [MachineIdentities]
      summary: List stale or expired-but-active machine tokens across all identities (admin)
      description: >-
        Deployment-wide machine-identity credentials that should probably be revoked:
        expired-but-never-revoked, and unused-for-a-long-window (non-human token
        sprawl). Requires global audit.read. Never returns a token secret.
      operationId: machineTokenHygiene
      security: [{bearerAuth: []}]
      parameters:
        - {name: days, in: query, schema: {type: integer}, description: "Staleness window in days (default server-side: 90, cap 3650)."}
      responses:
        '200':
          description: Envelope with `data.tokens` (array of machine token hygiene rows).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tokens: {type: array, items: {$ref: '#/components/schemas/MachineTokenHygieneRow'}}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/machine-identities/audit:
    get:
      tags: [MachineIdentities]
      summary: Deployment-wide machine identity audit report (admin)
      description: >-
        Credential counts, last-used timestamps, staleness, and revocation status
        for every machine identity in the deployment. Requires global audit.read.
      operationId: getMachineAuditReport
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope with `data` (the audit report).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/MachineAuditReport'}
        '403': {$ref: '#/components/responses/Error'}
  # docs/cli-split-inventory.md §7 PR 5 -- secret bulk/rotation/export/import/scan/hygiene.
  /api/v1/projects/{id}/secrets/expiring:
    get:
      tags: [Secrets]
      summary: List a project's secrets expiring (or already expired) within a window
      description: >-
        Returns the project's secrets expiring soon (or already expired), soonest-first.
        `days` defaults to 30, capped at 3650.
      operationId: listExpiringSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: days, in: query, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the expiring-secrets report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/ExpiringSecretsReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/orphaned:
    get:
      tags: [Secrets]
      summary: List a project's secrets whose owner is no longer a live user
      description: Offboarding hygiene report — secrets left without an accountable owner.
      operationId: listOrphanedSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the orphaned-secrets report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/OrphanedSecretsReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/name-conformance:
    get:
      tags: [Secrets]
      summary: List a project's secrets whose names violate the current naming policy
      description: >-
        The naming policy is enforced only at create time, so a name can fall out of
        conformance after the policy is added or tightened — this surfaces those stragglers.
      operationId: secretNameConformance
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the conformance report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretNameConformanceReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/reassign-owner:
    post:
      tags: [Secrets]
      summary: Re-home all of a departed user's secrets to a new owner
      description: >-
        Transfers every secret in the project owned by from_owner_id to to_owner_id in one
        call. Requires roles.assign at the project scope (the same blast radius as a role
        grant, not secrets.write).
      operationId: reassignSecretOwner
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from_owner_id, to_owner_id]
              properties:
                from_owner_id: {type: integer}
                to_owner_id: {type: integer}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.reassigned` is the count transferred.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      reassigned: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/bulk-rotate:
    post:
      tags: [Secrets]
      summary: Trigger rotation for multiple secrets at once
      description: >-
        Rotates every matching secret in the project (or an explicit secret_ids list, or
        env/classification filters). Secrets without auto-rotate configured are reported as
        failures, not treated as aborting errors — always best-effort.
      operationId: bulkRotateSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                secret_ids: {type: array, items: {type: integer}}
                environment_id: {type: integer}
                classification: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the bulk-rotate result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/BulkRotateResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/bulk-rename:
    post:
      tags: [Secrets]
      summary: Rename secrets toward naming-policy conformance
      description: >-
        Each new name must itself satisfy the policy and not collide with a sibling; an
        entry failing a check is skipped with a reason while the rest proceed. dry_run
        validates and reports without changing anything.
      operationId: bulkRenameSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [renames]
              properties:
                renames:
                  type: array
                  items:
                    type: object
                    properties:
                      id: {type: integer}
                      new_name: {type: string}
                dry_run: {type: boolean}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the bulk-rename report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/BulkRenameReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/bulk-delete:
    post:
      tags: [Secrets]
      summary: Delete multiple secrets in one operation
      description: >-
        Deletes every secret in secret_ids via the normal single-secret delete path;
        partial success is allowed — failures are collected rather than aborting the batch.
      operationId: bulkDeleteSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [secret_ids]
              properties:
                secret_ids: {type: array, items: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the bulk-delete result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/BulkDeleteResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/render:
    post:
      tags: [Secrets]
      summary: Render a template, expanding ${secret:<environment>/<name>} references
      description: >-
        Resolves every ${secret:<environment>/<name>} reference within the project under
        the caller's own read permissions. A missing or forbidden reference fails the
        render without returning partial output.
      operationId: renderSecretTemplate
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [template]
              properties:
                template: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.rendered` is the expanded template.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      rendered: {type: string}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/secrets:
    get:
      tags: [Secrets]
      summary: List secrets
      description: List secrets accessible to the caller with optional filters for project, environment, type, and classification.
      operationId: listSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: environment_id, in: query, schema: {type: integer}}
        - {name: search, in: query, schema: {type: string}}
        - {name: type, in: query, schema: {type: string}}
        - {name: classification, in: query, schema: {type: string, enum: [public, internal, confidential, restricted, unclassified]}, description: "Data-classification filter (A.5.12); 'unclassified' matches secrets with no label."}
        - {name: expires_before, in: query, schema: {type: string, format: date-time}, description: "RFC3339 — return only secrets with a non-null expiration before this time (expired or expiring soon)."}
        - {name: page, in: query, schema: {type: integer, default: 1}}
        - {name: page_size, in: query, schema: {type: integer, default: 20, maximum: 100}}
        - {name: show_owned_only, in: query, schema: {type: boolean}}
        - {name: show_shared_only, in: query, schema: {type: boolean}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` holds the paginated list
            (`secrets` array plus pagination fields).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secrets: {type: array, items: {$ref: '#/components/schemas/SecretListEntry'}}
                      total: {type: integer}
                      page: {type: integer}
                      page_size: {type: integer}
                      total_pages: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Create a secret
      description: Create a new secret with a name, value, type, and optional metadata in the specified environment.
      operationId: createSecret
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, value, environment_id, type]
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                value: {type: string}
                project_id: {type: integer}
                environment_id: {type: integer}
                type: {type: string}
                max_reads: {type: integer, minimum: 1, nullable: true}
                metadata: {type: object, additionalProperties: {type: string}}
                tags: {type: array, items: {type: string}}
                classification: {type: string, description: "Optional data-sensitivity label (A.5.12): public|internal|confidential|restricted."}
                description: {type: string, maxLength: 1024, description: "Optional free-text note. Metadata only -- never the value. Fixed missing from this spec (ADR-108 PR 4) -- the real handler (secrets_crud.go's CreateSecret) has accepted this field since #1808, this spec just never documented it."}
                expiration: {type: string, format: date-time, description: "Optional RFC3339 expiration. Fixed missing from this spec (ADR-108 PR 4) -- same #1808 gap as description above."}
      responses:
        '201':
          description: >-
            Envelope `{data, message}`. `data` is the created secret node
            (`id`, `name`, `project_id`, `environment_id`, `type`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}:
    get:
      tags: [Secrets]
      summary: Get a secret
      description: Return a secret by ID; include include_value=true to retrieve the decrypted value.
      operationId: getSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - name: include_value
          in: query
          description: When `true`, response `data` becomes `{secret, value}` with the decrypted value.
          schema: {type: boolean}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` is the secret node, or
            `{secret, value}` when `include_value=true`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretGetResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Secrets]
      summary: Update a secret
      description: Update a secret's value, max-read limit, or expiration by ID.
      operationId: updateSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: >-
                `expiration` (RFC3339) and `clear_expiration` are mutually
                exclusive.
              properties:
                value: {type: string}
                max_reads: {type: integer, minimum: 1, nullable: true}
                expiration: {type: string, format: date-time}
                clear_expiration: {type: boolean}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the updated secret node.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Secrets]
      summary: Delete a secret
      description: Permanently delete a secret and all its versions by ID.
      operationId: deleteSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Secret deleted (no content).}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/acl:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}, description: Secret ID}
    get:
      tags: [Secrets]
      summary: List ACL grants for a secret
      description: >-
        Returns all per-secret ACL entries for the given secret. Requires
        `secrets.manage` at the secret's project scope.
      operationId: listSecretACLs
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is an array of SecretACL objects."
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: {type: boolean}
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/SecretACL'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Grant a per-secret ACL entry
      description: >-
        Grants the specified permissions to a user on a single secret. The
        caller must hold `secrets.manage` at the secret's project scope. If
        an entry already exists for the (secret, user) pair it is replaced
        (upsert semantics).
      operationId: grantSecretACL
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, permissions]
              properties:
                user_id:
                  type: integer
                  description: ID of the user to grant access to.
                permissions:
                  type: array
                  items: {type: string}
                  description: "List of permission strings to grant, e.g. [\"secrets.read\"]."
                  minItems: 1
      responses:
        '200':
          description: "Envelope `data` is `{granted: true}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      granted: {type: boolean}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/acl/{aclId}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}, description: Secret ID}
      - {name: aclId, in: path, required: true, schema: {type: integer}, description: ACL entry ID}
    delete:
      tags: [Secrets]
      summary: Revoke a per-secret ACL entry
      description: >-
        Deletes the ACL entry identified by `aclId` for the given secret.
        Requires `secrets.manage` at the secret's project scope.
      operationId: revokeSecretACL
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is `{revoked: true}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      revoked: {type: boolean}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/classification:
    patch:
      tags: [Secrets]
      summary: Set a secret's data-classification label (ISO 27001 A.5.12)
      description: >-
        Label the secret with its data sensitivity (or empty to clear). Drives the
        compliance classification posture. Requires secrets.write at the secret's scope.
      operationId: classifySecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                classification:
                  type: string
                  description: "public|internal|confidential|restricted, or empty to clear."
      responses:
        '200':
          description: "Envelope {data, message}. data is the updated secret node."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/by-name:
    get:
      tags: [Secrets]
      summary: Get a secret's metadata by name
      description: >-
        Look up a secret's metadata (never the value) by name, scoped to a
        project/environment. For callers that only have the name, not the
        numeric ID.
      operationId: getSecretByName
      security: [{bearerAuth: []}]
      parameters:
        - {name: name, in: query, required: true, schema: {type: string}}
        - {name: project_id, in: query, required: true, schema: {type: integer}}
        - {name: environment_id, in: query, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the secret node.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/value:
    get:
      tags: [Secrets]
      summary: Read a secret's value by reference
      description: >-
        Resolve a "project/environment/name" reference (ADR-059) directly to
        its decrypted value -- for scripts/integrations (e.g. the External
        Secrets Operator) that template a key string instead of tracking a
        numeric ID. Always reads and audits the value, like any value read.
      operationId: getSecretValueByRef
      security: [{bearerAuth: []}]
      parameters:
        - {name: ref, in: query, required: true, schema: {type: string}, description: "project/environment/name"}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{secret, value}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secret: {$ref: '#/components/schemas/Secret'}
                      value: {type: string}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/versions/{from}/diff/{to}:
    get:
      tags: [Secrets]
      summary: Diff metadata between two versions of a secret
      description: >-
        Compare metadata (classification, expiry, read count, current ACL
        snapshot) between two versions of a secret. Values are never
        compared or returned.
      operationId: diffSecretVersions
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: from, in: path, required: true, schema: {type: integer}}
        - {name: to, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the version diff result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretVersionDiffResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/versions/{versionId}/comments:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
      - {name: versionId, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Secrets]
      summary: List comments on a secret version
      description: List all free-text comments on a specific secret version, oldest first.
      operationId: listSecretVersionComments
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{comments, total}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      comments: {type: array, items: {$ref: '#/components/schemas/SecretVersionComment'}}
                      total: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Add a comment to a secret version
      description: Add a free-text comment to a specific secret version.
      operationId: addSecretVersionComment
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [comment]
              properties:
                comment: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the created comment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretVersionComment'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/versions/{versionId}/comments/{commentId}:
    delete:
      tags: [Secrets]
      summary: Delete a comment from a secret version
      description: Delete a comment from a specific secret version by comment ID. Requires secrets.manage.
      operationId: deleteSecretVersionComment
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: versionId, in: path, required: true, schema: {type: integer}}
        - {name: commentId, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Comment deleted.}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/tags:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Secrets]
      summary: Get a secret's tags
      description: Metadata only -- never the value. Requires secrets.read.
      operationId: getSecretTags
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{tags}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tags: {type: array, items: {type: string}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Secrets]
      summary: Replace a secret's tags
      description: Replaces the full tag set. Requires secrets.write.
      operationId: setSecretTags
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tags]
              properties:
                tags: {type: array, items: {type: string}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{tags}` (the stored set).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tags: {type: array, items: {type: string}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/description:
    patch:
      tags: [Secrets]
      summary: Set a secret's free-text description
      description: Metadata only -- never the value. Requires secrets.write.
      operationId: describeSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description: {type: string, maxLength: 1024}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` is the updated secret node --
            declared as a generic object here (not $ref Secret) since no CLI
            command reads any field from it, only the status code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: object}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/move:
    post:
      tags: [Secrets]
      summary: Move a secret or folder to a different parent folder
      description: Use parent_id 0 to move the node to the root (no parent). Requires secrets.write.
      operationId: moveSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                parent_id: {type: integer, description: "0 = move to root."}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. Declared as a generic object -- no
            CLI command reads any field from the response, only the status code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: object}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/copy:
    post:
      tags: [Secrets]
      summary: Copy a secret's value and metadata into another environment
      description: >-
        Promote a secret into another environment of the same project without
        re-typing the value. Creates a new, independently-owned secret.
        Requires secrets.read on the source and secrets.write on the target.
      operationId: copySecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [environment_id]
              properties:
                environment_id: {type: integer}
                name: {type: string, description: "Name for the copy (default: the source name)."}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.ID` is the new secret's ID.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ID: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/dependencies:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Secrets]
      summary: Show what a secret depends on and what depends on it
      description: ADR-052 dependency graph. Drives rotation impact analysis and ordering.
      operationId: listSecretDependencies
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{secret_id, depends_on, dependents}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretDependencies'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Declare that this secret depends on another
      description: ADR-052 dependency graph edge. Drives rotation impact analysis and ordering. Requires secrets.write.
      operationId: addSecretDependency
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [depends_on_id]
              properties:
                depends_on_id: {type: integer}
                note: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the created dependency edge.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretDependencyEdge'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/dependencies/{depId}:
    delete:
      tags: [Secrets]
      summary: Remove a dependency edge
      description: Remove a dependency edge by ID (from the depends_on/dependents list). Requires secrets.write.
      operationId: removeSecretDependency
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: depId, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Dependency edge removed.}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/impact:
    get:
      tags: [Secrets]
      summary: Show the blast radius of rotating a secret
      description: Transitive dependents -- every secret that would be affected by rotating this one.
      operationId: getSecretImpact
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{secret_id, secret_name, affected}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretImpact'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/access:
    get:
      tags: [Secrets]
      summary: List who can read a secret
      description: Effective access list -- every user who can read the secret, with their permission and how it was granted (owner, direct share, group share).
      operationId: listAccessors
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{accessors}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      accessors: {type: array, items: {$ref: '#/components/schemas/SecretAccessor'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/access-log:
    get:
      tags: [Secrets]
      summary: Show a secret's recent reads
      description: >-
        Recent access-log entries -- who read it, when, from where. The originating
        IP/user-agent are included only for a caller who separately holds audit.read
        (checked globally); every other secrets.read caller sees accessor/action/time only.
      operationId: getSecretAccessLog
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: days, in: query, schema: {type: integer}, description: "Lookback window (default server-side: 30)."}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is `{access_log}`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      access_log: {type: array, items: {$ref: '#/components/schemas/SecretAccessLogEntry'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/schedule:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Secrets]
      summary: Get a secret's temporal access schedule
      description: Requires secrets.manage.
      operationId: getSecretSchedule
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the schedule.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretAccessSchedule'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Secrets]
      summary: Set a secret's temporal access schedule
      description: >-
        Restrict read access to a day-of-week + hour window. Applies to
        permission-checked human reads; admin/machine reads bypass it.
      operationId: setSecretSchedule
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [allowed_days, start_hour, end_hour]
              properties:
                allowed_days: {type: string, description: "Comma-separated ISO weekday list (1=Mon..7=Sun), or \"*\" for all days."}
                start_hour: {type: integer, minimum: 0, maximum: 23}
                end_hour: {type: integer, minimum: 1, maximum: 24}
                timezone: {type: string, description: "IANA timezone name, e.g. UTC or America/New_York."}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the stored schedule.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretAccessSchedule'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Secrets]
      summary: Remove a secret's temporal access schedule
      description: Idempotent.
      operationId: deleteSecretSchedule
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Schedule removed.}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/suspend:
    post:
      tags: [Secrets]
      summary: Suspend a secret
      description: >-
        Block value reads without deleting the secret. Versions, shares, and
        the audit trail are preserved. Requires secrets.write.
      operationId: suspendSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason: {type: string, description: "Optional reason recorded in the audit event."}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. Declared as a generic object -- no
            CLI command reads any field from it, only the status code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: object}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/resume:
    post:
      tags: [Secrets]
      summary: Resume a suspended secret
      description: Restore value reads. Requires secrets.write.
      operationId: resumeSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. Declared as a generic object -- no
            CLI command reads any field from it, only the status code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: object}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/rollback:
    post:
      tags: [Secrets]
      summary: Restore a secret to the value of a prior version
      description: >-
        The old value is re-instated as a NEW version -- history stays
        append-only, so the rollback itself can be undone. Requires secrets.write.
      operationId: rollbackSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [version]
              properties:
                version: {type: integer, description: "Version number to restore."}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. Declared as a generic object -- no
            CLI command reads any field from it, only the status code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: object}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/restore:
    post:
      tags: [Secrets]
      summary: Restore a soft-deleted secret
      description: >-
        Clears the deletion, returning the secret to the live list. Find
        restorable secrets with GET /projects/{id}/secrets/deleted.
      operationId: restoreSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: "Envelope `{data, message}`. `data` is `{id, restored}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id: {type: integer}
                      restored: {type: boolean}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/secrets/deleted:
    get:
      tags: [Secrets]
      summary: List a project's soft-deleted (restorable) secrets
      description: >-
        The recycle bin for a project -- secrets that were deleted but are
        still restorable until the purge job reaps them, newest-deleted
        first. Requires secrets.read at the project scope.
      operationId: listDeletedSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: limit, in: query, schema: {type: integer}, description: "Max entries (default server-side: 100, cap 500)."}
      responses:
        '200':
          description: "Envelope `{data, message}`. `data` is `{deleted}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      deleted: {type: array, items: {$ref: '#/components/schemas/DeletedSecretEntry'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/projects/{id}/environments/{envId}/copy-secrets:
    post:
      tags: [Secrets]
      summary: Copy every secret from one environment into another
      description: >-
        Bulk-promote a whole environment (e.g. seed production from staging)
        in a single call. Name clashes in the target are skipped, never
        overwritten. Requires secrets.write on the target, secrets.read on
        the source environment.
      operationId: copyEnvironmentSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}, description: Project ID.}
        - {name: envId, in: path, required: true, schema: {type: integer}, description: Source environment ID.}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_environment_id]
              properties:
                target_environment_id: {type: integer}
      responses:
        '200':
          description: "Envelope `{data, message}`. `data` is `{copied, skipped}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      copied: {type: integer}
                      skipped: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/folders:
    get:
      tags: [Secrets]
      summary: List folders
      description: Folders are secret nodes with IsSecret=false. Filterable by project and parent.
      operationId: listFolders
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: parent_id, in: query, schema: {type: integer}, description: "0/omit = no filter."}
        - {name: page_size, in: query, schema: {type: integer}}
      responses:
        '200':
          description: "Envelope `data` is an array of folder nodes."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {type: array, items: {$ref: '#/components/schemas/Secret'}}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Create a folder
      description: Authorized in-handler against the project/environment in the body.
      operationId: createFolder
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, environment_id]
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                project_id: {type: integer}
                environment_id: {type: integer}
                parent_id: {type: integer, description: "0/omit = root."}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the created folder node.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/folders/{id}:
    delete:
      tags: [Secrets]
      summary: Delete a folder
      description: Requires secrets.delete at the folder's scope. Refuses if the target node is a secret, not a folder.
      operationId: deleteFolder
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Folder deleted.}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secret-templates:
    get:
      tags: [Secrets]
      summary: List secret templates
      description: Reusable metadata presets (tags, classification, description hints) for secret creation. Requires secrets.read.
      operationId: listSecretTemplates
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `{data, message}`. `data` is `{templates}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      templates: {type: array, items: {$ref: '#/components/schemas/SecretTemplate'}}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Secrets]
      summary: Create a secret template
      description: Requires secrets.write. Templates are deployment-wide, not project-scoped.
      operationId: createSecretTemplate
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                description: {type: string}
                default_classification: {type: string}
                default_tags: {type: string, description: "Comma-separated tag names."}
                description_pattern: {type: string}
                rotation_hint_days: {type: integer}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the created template.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretTemplate'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/secret-templates/{id}:
    delete:
      tags: [Secrets]
      summary: Delete a secret template
      description: Requires secrets.write.
      operationId: deleteSecretTemplate
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Template deleted.}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/rotate:
    post:
      tags: [Secrets]
      summary: Rotate a secret value
      description: Replace a secret's current value with a new one, archiving the previous version.
      operationId: rotateSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [new_value]
              properties:
                new_value: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the rotated secret node.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Secret'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/rotation/simulate:
    post:
      tags: [Secrets]
      summary: Simulate a rotation dry-run for a secret
      description: >-
        Validates the secret's rotation configuration (policy, backend, ref) without
        executing any live rotation. Read-only — requires only secrets.read.
      operationId: simulateSecretRotation
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the dry-run result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/RotationDryRunResult'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/auto-rotate:
    patch:
      tags: [Secrets]
      summary: Enable/disable automated rotation for a secret
      description: >-
        Opts a secret into automated rotation on schedule, optionally rotating an
        upstream credential too when backend/ref are set. Enable only for secrets whose
        value Keyorix owns.
      operationId: setSecretAutoRotate
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: {type: boolean}
                length: {type: integer}
                charset: {type: string}
                backend: {type: string}
                ref: {type: string}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` echoes the applied configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      id: {type: integer}
                      auto_rotate: {type: boolean}
                      length: {type: integer}
                      charset: {type: string}
                      backend: {type: string}
                      ref: {type: string}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/audit:
    get:
      tags: [Secrets]
      summary: Show a secret's lifecycle events
      description: >-
        Lists the audit trail for a secret (created/rotated/rolled-back/suspended/
        resumed/shared/owner-transferred/reclassified, …), newest first. Never
        includes a plaintext value. `limit` defaults to 50, capped at 500.
      operationId: getSecretAuditTrail
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - {name: limit, in: query, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.audit` is the event list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      audit: {type: array, items: {$ref: '#/components/schemas/SecretAuditEntry'}}
                      total: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/ownership-history:
    get:
      tags: [Secrets]
      summary: Show a secret's ownership-transfer chain
      description: Ordered list of ownership transfers for a secret (oldest first).
      operationId: getSecretOwnershipHistory
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.ownership_history` is the transfer list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      ownership_history: {type: array, items: {$ref: '#/components/schemas/OwnershipRecord'}}
                      total: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/certificate:
    get:
      tags: [Secrets]
      summary: Show a certificate secret's public X.509 metadata
      description: >-
        Subject, issuer, validity window, SANs, and algorithms of a certificate-valued
        secret. Never returns the value or any private key.
      operationId: getSecretCertificate
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the certificate metadata.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/CertificateInfo'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/blast-radius:
    get:
      tags: [Secrets]
      summary: Show the enriched blast radius of rotating or deleting a secret
      description: >-
        All downstream dependents of a secret (recursive), each annotated with owner,
        project, hop depth, and risk level.
      operationId: getSecretBlastRadius
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the blast-radius report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/BlastRadiusReport'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/versions:
    get:
      tags: [Secrets]
      summary: List secret versions
      description: Return the full version history for a secret, including previous values' metadata.
      operationId: getSecretVersions
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.versions` is the version history array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      versions: {type: array, items: {$ref: '#/components/schemas/SecretVersion'}}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/risk:
    get:
      tags: [Secrets]
      summary: Get a secret's risk score
      description: Return the composite risk score for a secret based on expiry, rotation, usage, and exposure factors.
      operationId: getSecretRisk
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` is the composite risk score
            (expiry / rotation / usage / exposure factors).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/SecretRiskScore'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/quota-report:
    get:
      tags: [Secrets]
      summary: Show secrets approaching or at their MaxReads quota
      description: >-
        Lists every live secret with a MaxReads cap configured: current read count,
        quota percentage, and status. Requires audit.read (a usage-report endpoint,
        not secret-value access) -- NOT secrets.read.
      operationId: getQuotaReport
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data.secrets` is the quota report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secrets: {type: array, items: {$ref: '#/components/schemas/QuotaReportRow'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/name-conformance:
    get:
      tags: [Secrets]
      summary: List every project's secrets whose names violate the naming policy (org-wide)
      description: >-
        Deployment-wide naming-policy conformance report, each violation tagged with its
        project. Requires audit.read (global) -- NOT the baseline system.read.
      operationId: deploymentSecretNameConformance
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the org-wide conformance report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/DeploymentSecretNameConformanceReport'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/usage/most-accessed:
    get:
      tags: [Secrets]
      summary: List most-accessed secrets
      description: Return the top secrets ranked by read count within the specified project and time window.
      operationId: getMostAccessedSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: days, in: query, schema: {type: integer, default: 30}}
        - {name: limit, in: query, schema: {type: integer, default: 10}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data.secrets` ranks secrets by read
            count; `data.days` echoes the window.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/usage/unused:
    get:
      tags: [Secrets]
      summary: List unused secrets
      description: Return secrets that have not been read within the specified number of days, never-read first.
      operationId: getUnusedSecrets
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: days, in: query, schema: {type: integer, default: 30}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data.secrets` lists secrets unread in
            the last N days (never-read first); `data.days` echoes the window.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/shares:
    get:
      tags: [Shares]
      summary: List a secret's shares
      description: Return all share records for a secret, showing who has been granted access and at what permission level.
      operationId: listSecretShares
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.shares` is the share records array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      shares: {type: array, items: {$ref: '#/components/schemas/Share'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/share:
    post:
      tags: [Shares]
      summary: Share a secret
      description: Grant read or write access to a secret for a specific user or group.
      operationId: shareSecret
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [recipient_id, permission]
              properties:
                recipient_id:
                  type: integer
                  description: User ID, or group ID when `is_group` is true.
                is_group: {type: boolean}
                permission: {type: string, enum: [read, write]}
                expires_at:
                  type: string
                  format: date-time
                  description: Optional time-bound (JIT) share expiry. Omitted = permanent.
      responses:
        '201':
          description: Envelope `{data, message}`. `data` is the created share record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Share'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/secrets/{id}/self-share:
    delete:
      tags: [Shares]
      summary: Remove yourself from a secret shared with you
      description: >-
        Self-service removal of the caller's own share grant (RecipientID ==
        caller), distinct from `revokeShare` (owner-driven revocation of
        anyone's share).
      operationId: removeSelfFromShare
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Removed (no content).}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/secrets/{id}/access-log/export:
    get:
      tags: [Secrets]
      summary: Download secret access log as a file
      description: >-
        Returns the secret's full access history (secret.read audit events) as
        a downloadable file. Supports JSON (default) and CSV formats via the
        `format` query parameter. Gated by secrets.read at the secret's project
        scope. Each row's ip_address is populated only for a caller who
        separately holds audit.read (checked globally), matching GET
        .../access-log's own gate; every other row field is unconditional.
      operationId: exportSecretAccessLog
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
        - name: format
          in: query
          schema: {type: string, enum: [json, csv], default: json}
          description: "Output format. `json` returns a JSON array; `csv` returns a spreadsheet-friendly attachment."
      responses:
        '200':
          description: File download. Content-Type is `application/json` or `text/csv` depending on `format`.
          content:
            application/json:
              schema: {type: string, format: binary}
            text/csv:
              schema: {type: string, format: binary}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/dynamic-secrets/configs:
    get:
      tags: [DynamicSecrets]
      summary: List dynamic-secret configs
      description: >-
        List dynamic-secret configs, optionally filtered by project/environment.
        Never includes the encrypted admin DSN. Added for ADR-108 PR 1 (thin CLI
        `dynamic-secret list`) — this route existed and was already called by the
        old CLI's remote mode; it was simply undocumented in this spec until now.
      operationId: listDynamicSecretConfigs
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: environment_id, in: query, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is the array of sanitized configs (never the admin DSN).
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/DynamicSecretConfig'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [DynamicSecrets]
      summary: Create a dynamic-secret config
      description: >-
        Register a target backend (database DSN, cloud IAM, etc.) that this
        config can mint short-lived credentials against. Added for ADR-108 PR 1
        (thin CLI `dynamic-secret create`) — see listDynamicSecretConfigs above.
      operationId: createDynamicSecretConfig
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, project_id, environment_id, backend_type, admin_dsn, creation_template]
              properties:
                name: {type: string}
                project_id: {type: integer}
                environment_id: {type: integer}
                backend_type: {type: string}
                admin_dsn: {type: string, description: "Plaintext admin DSN; encrypted at rest, never echoed back"}
                creation_template: {type: string}
                default_ttl_seconds: {type: integer}
                max_ttl_seconds: {type: integer}
                max_active_leases: {type: integer}
                classification: {type: string, enum: ['', public, internal, confidential, restricted]}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is the sanitized created config.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/DynamicSecretConfig'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/configs/{id}:
    get:
      tags: [DynamicSecrets]
      summary: Get a dynamic-secret config
      description: >-
        Return a single sanitized config by ID. Added for ADR-108 PR 1 (thin CLI
        `dynamic-secret get-config`) — see listDynamicSecretConfigs above.
      operationId: getDynamicSecretConfig
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is the sanitized config.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/DynamicSecretConfig'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/configs/{id}/classification:
    patch:
      tags: [DynamicSecrets]
      summary: Set a dynamic-secret config's data-classification label
      description: >-
        Added for ADR-108 PR 1 (thin CLI `dynamic-secret classify`) — see
        listDynamicSecretConfigs above.
      operationId: classifyDynamicSecretConfig
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                classification: {type: string, enum: ['', public, internal, confidential, restricted]}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is the updated sanitized config.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/DynamicSecretConfig'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/configs/{id}/issue:
    post:
      tags: [DynamicSecrets]
      summary: Issue a short-lived credential (lease) from a config
      description: >-
        Mints a new credential against the config's backend and returns it once
        (it is never retrievable again). Added for ADR-108 PR 1 (thin CLI
        `dynamic-secret issue`) — see listDynamicSecretConfigs above.
      operationId: issueDynamicSecretLease
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ttl_seconds: {type: integer}
      responses:
        '200':
          description: >-
            Envelope `{success, data, message}`. `data`: `{lease_id, username?,
            password?, fields?, expires_at}` — the one-time credential.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/DynamicSecretIssuedLease'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '502': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/configs/{id}/leases:
    get:
      tags: [DynamicSecrets]
      summary: List a config's leases
      description: >-
        Added for ADR-108 PR 1 (thin CLI `dynamic-secret leases`) — see
        listDynamicSecretConfigs above. Never includes the issued credential
        itself (that is shown once, at issue time, and never again).
      operationId: listDynamicSecretLeases
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{success, data, message}`. `data` is the array of sanitized leases.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/DynamicSecretLease'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/configs/{id}/revoke-all:
    post:
      tags: [DynamicSecrets]
      summary: Revoke every active lease issued from a config
      description: >-
        The incident kill switch. Added for ADR-108 PR 1 (thin CLI
        `dynamic-secret revoke-all`) — see listDynamicSecretConfigs above.
      operationId: revokeAllDynamicSecretLeases
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: "Envelope `{success, data, message}`. `data`: `{config_id, revoked, failed}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      config_id: {type: integer, format: uint32}
                      revoked: {type: integer}
                      failed: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '502': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/leases/{leaseID}/renew:
    post:
      tags: [DynamicSecrets]
      summary: Extend a lease's expiry
      description: >-
        Added for ADR-108 PR 1 (thin CLI `dynamic-secret renew`) — see
        listDynamicSecretConfigs above.
      operationId: renewDynamicSecretLease
      security: [{bearerAuth: []}]
      parameters:
        - {name: leaseID, in: path, required: true, schema: {type: string}}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ttl_seconds: {type: integer}
      responses:
        '200':
          description: "Envelope `{success, data, message}`. `data`: `{lease_id, expires_at}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      lease_id: {type: string}
                      expires_at: {type: string}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '502': {$ref: '#/components/responses/Error'}
  /api/v1/dynamic-secrets/leases/{leaseID}/revoke:
    post:
      tags: [DynamicSecrets]
      summary: Revoke a single lease
      description: >-
        Added for ADR-108 PR 1 (thin CLI `dynamic-secret revoke`) — see
        listDynamicSecretConfigs above.
      operationId: revokeDynamicSecretLease
      security: [{bearerAuth: []}]
      parameters:
        - {name: leaseID, in: path, required: true, schema: {type: string}}
      responses:
        '200':
          description: "Envelope `{success, data, message}`. `data`: `{lease_id, status}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: object
                    properties:
                      lease_id: {type: string}
                      status: {type: string}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '502': {$ref: '#/components/responses/Error'}

  /api/v1/shares:
    get:
      tags: [Shares]
      summary: List shares created by the caller
      description: Return all secret shares that were created by the authenticated user.
      operationId: listShares
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data.shares` is the share records array.
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/shares/{id}:
    put:
      tags: [Shares]
      summary: Update a share's permission
      description: Change the permission level (read or write) of an existing secret share.
      operationId: updateSharePermission
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [permission]
              properties:
                permission: {type: string, enum: [read, write]}
                expires_at:
                  type: string
                  format: date-time
                  description: Set/extend/shorten the time-bound expiry. Mutually exclusive with clear_expiry.
                clear_expiry:
                  type: boolean
                  description: Make the share permanent (remove its expiry). Mutually exclusive with expires_at.
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the updated share record.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Share'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Shares]
      summary: Revoke a share
      description: Remove a secret share, revoking the recipient's access to the secret.
      operationId: revokeShare
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Share revoked (no content).}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/shared-secrets:
    get:
      tags: [Shares]
      summary: List secrets shared with the caller
      description: Return all secrets that other users or groups have shared with the authenticated user.
      operationId: listSharedSecrets
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data.secrets` is the shared secret nodes array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secrets: {type: array, items: {$ref: '#/components/schemas/Secret'}}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/rotation-policies:
    get:
      tags: [RotationPolicies]
      summary: List rotation policies
      description: Return rotation policies, optionally filtered by project or environment.
      operationId: listRotationPolicies
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
        - {name: environment_id, in: query, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the rotation policies array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: array
                    items: {$ref: '#/components/schemas/RotationPolicy'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [RotationPolicies]
      summary: Create a rotation policy
      description: Create a new rotation policy with a name, scope, interval, and optional alert thresholds.
      operationId: createRotationPolicy
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, scope, interval_days]
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                description: {type: string}
                scope: {type: string, enum: [project, environment]}
                project_id: {type: integer, nullable: true}
                environment_id: {type: integer, nullable: true}
                interval_days: {type: integer, minimum: 1, maximum: 365}
                alert_days_before: {type: integer, minimum: 0, maximum: 90}
                notify_on_breach: {type: boolean}
      responses:
        '201':
          description: Envelope `{data, message}`. `data` is the created rotation policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/RotationPolicy'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/rotation-policies/evaluate:
    get:
      tags: [RotationPolicies]
      summary: Evaluate rotation policies
      description: Evaluate rotation policies and return secrets that are overdue or approaching their rotation deadline.
      operationId: evaluateRotationPolicies
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` lists secrets that are overdue or
            approaching their rotation deadline.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data:
                    type: array
                    nullable: true
                    description: >-
                      null (not []) when EvaluateRotationPolicies' `var
                      evaluations []*RotationPolicyEvaluation` never gets
                      appended to -- e.g. no active policy covers anything
                      currently overdue or approaching. Verified against the
                      real handler, not asserted: this is exactly the shape
                      TestContractPR1_EvaluateRotationPolicies
                      (openapi_contract_pr1_test.go) exercises.
                    items: {$ref: '#/components/schemas/RotationPolicyEvaluation'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/rotation-policies/status:
    get:
      tags: [RotationPolicies]
      summary: Get rotation posture per secret
      description: Return rotation posture (overdue, due soon, or OK) for every policy-covered secret in the project.
      operationId: getRotationStatus
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}}
      responses:
        '200':
          description: >-
            Envelope `{data, message}`. `data` reports rotation posture
            (`overdue` / `due_soon` / `ok`) for every policy-covered secret.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/rotation-policies/{id}:
    get:
      tags: [RotationPolicies]
      summary: Get a rotation policy
      description: Return a single rotation policy by its numeric ID.
      operationId: getRotationPolicy
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the rotation policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/RotationPolicy'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [RotationPolicies]
      summary: Update a rotation policy
      description: Update a rotation policy's name, interval, alert thresholds, or active status.
      operationId: updateRotationPolicy
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, interval_days]
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                description: {type: string}
                interval_days: {type: integer, minimum: 1, maximum: 365}
                alert_days_before: {type: integer, minimum: 0, maximum: 90}
                notify_on_breach: {type: boolean}
                is_active: {type: boolean}
      responses:
        '200':
          description: Envelope `{data, message}`. `data` is the updated rotation policy.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [RotationPolicies]
      summary: Delete a rotation policy
      description: Permanently delete a rotation policy by ID.
      operationId: deleteRotationPolicy
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '204': {description: Rotation policy deleted (no content).}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/rotation-plan:
    get:
      tags: [RotationPolicies]
      summary: Get the install-wide rotation plan (ADR-053)
      description: >-
        Every project's overdue/due-soon secrets aggregated into a roll-up,
        most-pressing project first. Gated by GLOBAL secrets.read (the same
        access level as listing all projects) — deliberately a step up from
        the per-project route above, documented, not a gap (docs/cli-split-
        inventory.md §2.4). Added for ADR-108 PR 1 (thin CLI `rotation plan
        --all-projects`) — see getProjectRotationOrder above.
      operationId: getDeploymentRotationPlan
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope `{success, data, message}`. `data` is a
            DeploymentRotationPlan.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: {type: string}
                  data: {$ref: '#/components/schemas/DeploymentRotationPlan'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/users:
    get:
      tags: [Users]
      summary: List users
      description: Return a paginated list of users with optional filters for search, state, and activity.
      operationId: listUsers
      security: [{bearerAuth: []}]
      parameters:
        - {name: page, in: query, schema: {type: integer, default: 1}}
        - {name: page_size, in: query, schema: {type: integer, default: 20, maximum: 100}}
        - {name: search, in: query, schema: {type: string}}
        - {name: username, in: query, schema: {type: string}}
        - {name: email, in: query, schema: {type: string}}
        - {name: is_active, in: query, schema: {type: boolean}}
        - {name: include_deleted, in: query, schema: {type: boolean}}
        - {name: filter, in: query, description: "Set to `inactive` for users with no login in the last 30 days.", schema: {type: string, enum: [inactive]}}
      responses:
        '200':
          description: "Envelope `data` has `users[]` (each with id, username, email, display_name, project_count, active_project_count), `page`, `page_size`, `total`, `total_pages`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      users: {type: array, items: {$ref: '#/components/schemas/UserSummary'}}
                      page: {type: integer}
                      page_size: {type: integer}
                      total: {type: integer}
                      total_pages: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Users]
      summary: Create user
      description: Create a new user with credentials, optionally delivering a setup link or one-time password.
      operationId: createUser
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, email, display_name]
              properties:
                username: {type: string, minLength: 3, maxLength: 50}
                email: {type: string, format: email}
                display_name: {type: string, minLength: 1, maxLength: 100}
                password: {type: string, minLength: 8, description: "Required unless deliver_setup_link or generate_one_time_password is set."}
                is_active: {type: boolean}
                deliver_setup_link: {type: boolean, description: "Provision via account-setup link instead of an admin-set password."}
                generate_one_time_password: {type: boolean, description: "Server-generated one-time password, returned once. Mutually exclusive with deliver_setup_link."}
                role: {type: string, description: "Optional system role override (admin-set-password path only)."}
                project_assignments:
                  type: array
                  description: "Project-scoped role assignments applied atomically (admin-set-password path only)."
                  items:
                    type: object
                    properties:
                      project_id: {type: integer}
                      role: {type: string}
      responses:
        '201':
          description: "Envelope `data` is the created user. With generate_one_time_password it is `{user, one_time_password}`; with deliver_setup_link it is `{user, setup_link}`."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/users/search:
    get:
      tags: [Users]
      summary: Search users
      description: Search users by a query string, returning up to ten matching results.
      operationId: searchUsers
      security: [{bearerAuth: []}]
      parameters:
        - {name: q, in: query, required: true, schema: {type: string}}
      responses:
        '200':
          description: "Envelope `data.users[]` with id, username, email (max 10)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/users/stale:
    get:
      tags: [Users]
      summary: List stale (stuck-state) accounts
      description: Return user accounts stuck in pending_first_login or password_reset_required state beyond the specified age.
      operationId: listStaleUsers
      security: [{bearerAuth: []}]
      parameters:
        - {name: state, in: query, description: "Account state to filter.", schema: {type: string, enum: [pending_first_login, password_reset_required], default: pending_first_login}}
        - {name: days, in: query, description: "Minimum age in days in the restricted state.", schema: {type: integer, default: 7}}
      responses:
        '200':
          description: "Envelope `data` has `users[]`, `state`, `days`."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/users/by-email:
    get:
      tags: [Users]
      summary: Get a user by email (ADR-108 PR 6 addition)
      description: >-
        Return a single user by email address, for a caller that only has the
        email, not the numeric ID. Same gate (users.read) and not-found shape as
        GET /users/{id} -- grants no new capability, since users.read already lets
        a caller enumerate every user (including email) via GET /users. Added for
        ADR-108 PR 6 (thin CLI `user get --email`) -- was already a live route
        (handlers.GetUserByEmail, router.go), just undocumented in this spec until
        now.
      operationId: getUserByEmail
      security: [{bearerAuth: []}]
      parameters:
        - {name: email, in: query, required: true, schema: {type: string}}
      responses:
        '200':
          description: "Envelope `data` is the user (id, username, email, display_name, active, created_at, updated_at)."
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Users]
      summary: Get user
      description: Return a single user by ID, including project membership counts and account state.
      operationId: getUser
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is the user (id, username, email, display_name, project_count, active_project_count)."
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Users]
      summary: Update user
      description: Update a user's username, email, display name, or active status by ID.
      operationId: updateUser
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                username: {type: string, minLength: 3, maxLength: 50}
                email: {type: string, format: email}
                display_name: {type: string, minLength: 1, maxLength: 100}
                active: {type: boolean}
      responses:
        '200':
          description: "Envelope `data` is the updated user."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Users]
      summary: Delete (soft-delete) user
      description: Soft-delete a user account by ID, preserving audit history while preventing login.
      operationId: deleteUser
      security: [{bearerAuth: []}]
      responses:
        '204': {description: User deleted.}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/restore:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Restore soft-deleted user
      description: Restore a previously soft-deleted user account, re-enabling their access.
      operationId: restoreUser
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope with `message`; `data` is null."}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/suspend:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Suspend user
      description: Suspend a user account, immediately preventing authentication without deleting the account.
      operationId: suspendUser
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope with `message`; `data` is null."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/reactivate:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Reactivate user
      description: Reactivate a suspended user account, restoring their ability to authenticate.
      operationId: reactivateUser
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope with `message`; `data` is null."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/require-password-reset:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Require password reset on next login
      description: Flag a user account so the user must change their password before they can proceed after next login.
      operationId: requirePasswordReset
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope with `message`; `data` is null."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/resend-setup-link:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Reissue and resend account-setup link
      description: Generate a new account-setup link for a user and resend it via the configured delivery channel.
      operationId: resendSetupLink
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is the delivery outcome (includes the link itself in out-of-band mode)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/revoke-sessions:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    post:
      tags: [Users]
      summary: Force-logout a user (ADR-108 PR 6 addition)
      description: >-
        Terminate every active session of a user immediately, without changing
        their account state. Requires users.write; refuses to target the caller's
        own account (400). Added for ADR-108 PR 6 (thin CLI `user revoke-sessions`)
        -- was already a live route (UserHandler.RevokeSessions, router.go), just
        undocumented in this spec until now.
      operationId: revokeUserSessions
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is {revoked: <count>}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/roles:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Users]
      summary: Get user roles
      description: Return all roles assigned to the specified user.
      operationId: getUserRolesForUser
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data.roles[]` with id, name."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      roles: {type: array, items: {$ref: '#/components/schemas/RoleRef'}}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Users]
      summary: Replace user roles at a scope
      description: Replace the user's role assignments at the given project or global scope with the provided role IDs.
      operationId: updateUserRoles
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                role_ids: {type: array, items: {type: integer}}
                project_id: {type: integer, description: "Scope (0 = global)."}
                environment_id: {type: integer, description: "Scope (0 = global)."}
      responses:
        '200':
          description: "Envelope `data.roles[]` with id, name (post-replacement)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/memberships:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Users]
      summary: List user project memberships
      description: Return all project memberships for the specified user with their role and state in each project.
      operationId: getUserMembershipsForUser
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data.memberships[]` with project_id, project_name, role, state."
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/users/{id}/shared-secrets:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Users]
      summary: List secrets shared with a specific user (admin-scoped)
      description: >-
        Return all secrets shared with the specified user — the same data as
        GET /api/v1/shared-secrets (the caller's own shares), for an arbitrary
        target. A self-view (id == the caller) needs only secrets.read;
        viewing another user's shares additionally requires the S1
        admin-rank ceiling (the caller must hold, at every scope, every
        permission the target already holds). A target the caller lacks
        authority over and a target that does not exist both return the same
        403 — this route never confirms via status code whether a given user
        ID exists.
      operationId: listSharedSecretsForUser
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: Envelope `{data, message}`. `data.secrets` is the shared secret nodes array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secrets: {type: array, items: {$ref: '#/components/schemas/Secret'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/admin/jobs/suspend-inactive-users:
    post:
      tags: [Users]
      summary: Suspend users inactive beyond a threshold (ADR-108 PR 6 addition)
      description: >-
        Suspend all non-admin users whose last login (or account creation, if they
        have never logged in) is older than inactive_days days. dry_run evaluates
        without making changes. Requires system.write (deployment-wide sweep, not a
        resource-scoped users.* permission). Added for ADR-108 PR 6 (thin CLI `user
        suspend-inactive`) -- was already a live route
        (adminJobsHandler.SuspendInactiveUsers, router.go), just undocumented in
        this spec until now.
      operationId: suspendInactiveUsers
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [inactive_days]
              properties:
                inactive_days: {type: integer, description: "Inactivity threshold in days (must be > 0)."}
                dry_run: {type: boolean, description: "Preview which users would be suspended without making changes."}
      responses:
        '200':
          description: "Envelope `data` is {suspended: [<user ids>], skipped: <count>, total: <count>}."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/admin/impersonate:
    post:
      tags: [Users]
      summary: Start impersonating a user
      description: Issue a short-lived impersonation session token to act as another user for administrative purposes.
      operationId: startImpersonation
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id]
              properties:
                user_id: {type: integer}
      responses:
        '200':
          description: "Envelope `data` is a short-lived target session: token, expires_at, user_id, username, display_name, impersonated_by."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/groups:
    get:
      tags: [Groups]
      summary: List groups
      description: Return all groups in the deployment with their names and descriptions.
      operationId: listGroups
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` has `groups[]` (id, name, description) and `total`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      groups: {type: array, items: {$ref: '#/components/schemas/Group'}}
                      total: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Groups]
      summary: Create group
      description: Create a new group with a unique name and optional description.
      operationId: createGroup
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                description: {type: string}
      responses:
        '201':
          description: "Envelope `data` is the created group (id, name, description)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Group'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/groups/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Groups]
      summary: Get group
      description: Return a single group by its numeric ID, including name and description.
      operationId: getGroup
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is the group (id, name, description)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Group'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Groups]
      summary: Update group
      description: Update the name and/or description of an existing group by ID.
      operationId: updateGroup
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name: {type: string, minLength: 1, maxLength: 255}
                description: {type: string}
      responses:
        '200':
          description: "Envelope `data` is the updated group."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: {$ref: '#/components/schemas/Group'}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Groups]
      summary: Delete group
      description: Permanently delete a group by ID, removing all its memberships and role assignments.
      operationId: deleteGroup
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Group deleted.}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/groups/{id}/members:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Groups]
      summary: List group members
      description: Return all users who are members of the specified group.
      operationId: getGroupMembers
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` has `members[]` (user objects) and `total`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      members: {type: array, items: {$ref: '#/components/schemas/UserSummary'}}
                      total: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Groups]
      summary: Add group member
      description: Add a user to a group, optionally scoped to a specific project.
      operationId: addGroupMember
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id]
              properties:
                user_id: {type: integer}
                project_id:
                  type: integer
                  default: 0
                  description: "0 = global membership (default); non-zero = scoped to that project only."
      responses:
        '200':
          description: "Envelope `data` is `{group_id, user_id, project_id}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      group_id: {type: integer}
                      user_id: {type: integer}
                      project_id: {type: integer}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/groups/{id}/members/{userId}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
      - {name: userId, in: path, required: true, schema: {type: integer}}
      - {name: project_id, in: query, schema: {type: integer}, description: "Scope to the same project the membership was added with (0/omit = global membership). Fixed missing from this spec (ADR-108 PR 3) -- the real handler (groups_members.go's RemoveGroupMember) has always read this query param; every other caller sent it, this spec just never documented it."}
    delete:
      tags: [Groups]
      summary: Remove group member
      description: Remove a user from a group, revoking any access derived from that group membership.
      operationId: removeGroupMember
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Member removed.}
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/groups/{id}/roles:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Groups]
      summary: List group role assignments
      description: Return all role assignments for the specified group, showing each role and its scope.
      operationId: getGroupRoles
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is `{group_id, roles}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      group_id: {type: integer}
                      roles: {type: array, nullable: true, items: {$ref: '#/components/schemas/GroupRoleGrant'}, description: "nil (JSON null), not [], when the group has no role grants."}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Groups]
      summary: Assign role to group at a scope
      description: Grant a role to a group at the specified project or global scope, with an optional expiry for JIT access.
      operationId: assignRoleToGroup
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role_id]
              properties:
                role_id: {type: integer}
                project_id: {type: integer, description: "Scope (0 = global)."}
                environment_id: {type: integer, description: "Scope (0 = global)."}
                expires_at: {type: string, format: date-time, nullable: true, description: "RFC3339; omit or null for a non-expiring grant. JIT access — the assignment is automatically revoked when the timestamp is reached."}
      responses:
        '201':
          description: "Envelope `data` is `{group_id, role_id}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      group_id: {type: integer}
                      role_id: {type: integer}
                      expires_at: {type: string, format: date-time, nullable: true}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/groups/{id}/roles/{roleId}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
      - {name: roleId, in: path, required: true, schema: {type: integer}}
      - {name: project_id, in: query, schema: {type: integer}, description: "Scope (0 = global)."}
      - {name: environment_id, in: query, schema: {type: integer}, description: "Scope (0 = global)."}
    delete:
      tags: [Groups]
      summary: Remove role from group
      description: Revoke a role assignment from a group at the specified project or global scope.
      operationId: removeRoleFromGroup
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Role removed from group.}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  # docs/cli-split-inventory.md §7 PR 9 -- share.
  /api/v1/groups/{id}/shares:
    get:
      tags: [Groups, Shares]
      summary: List shares granted to a group
      description: >-
        The share grants made TO a group (as opposed to
        GET /groups/{id}/shared-secrets' resolved-secrets view). Requires
        secrets.read (global or at the group's shares' scope).
      operationId: listGroupShares
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: Envelope `{data, message}`. `data.shares` is the share records array.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      shares: {type: array, items: {$ref: '#/components/schemas/Share'}}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/roles:
    get:
      tags: [Roles]
      summary: List roles with permissions
      description: Return all roles in the deployment, each with its assigned permissions.
      operationId: listRoles
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` has `roles[]` (with permissions) and `total`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      roles: {type: array, items: {$ref: '#/components/schemas/RoleWithPermissions'}}
                      total: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
    post:
      tags: [Roles]
      summary: Create role
      description: Create a new custom role with a name, description, and initial set of permissions.
      operationId: createRole
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, description, permissions]
              properties:
                name: {type: string, minLength: 3, maxLength: 50}
                description: {type: string, minLength: 1, maxLength: 200}
                permissions: {type: array, minItems: 1, items: {type: string}, description: "Permission names."}
      responses:
        '201':
          description: "Envelope `data` is `{role, permissions}` (assigned permissions)."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}

  /api/v1/roles/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Roles]
      summary: Get role with permissions
      description: Return a single role by ID, including its full list of assigned permissions.
      operationId: getRole
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is `{role, permissions}`."
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Roles]
      summary: Update role
      description: Update a role's description and/or replace its full set of permissions.
      operationId: updateRole
      security: [{bearerAuth: []}]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                description: {type: string, minLength: 1, maxLength: 200}
                permissions: {type: array, minItems: 1, items: {type: string}, description: "Replaces the role's permissions by name."}
      responses:
        '200':
          description: "Envelope `data` is `{role, permissions}`."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Roles]
      summary: Delete role
      description: Permanently delete a role by ID; fails if the role is currently assigned to any user or group.
      operationId: deleteRole
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Role deleted.}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/roles/{id}/permissions:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Roles]
      summary: List role permissions
      description: Return all permissions currently assigned to the specified role.
      operationId: getRolePermissions
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is `{role_id, role_name, permissions}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      role_id: {type: integer}
                      role_name: {type: string}
                      permissions: {type: array, items: {$ref: '#/components/schemas/Permission'}}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    post:
      tags: [Roles]
      summary: Assign permission to role
      description: Add a permission to the specified role, granting it to all users and groups holding that role.
      operationId: assignPermissionToRole
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [permission_id]
              properties:
                permission_id: {type: integer}
      responses:
        '201':
          description: "Envelope `data` is `{role_id, permission_id}`."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/roles/{id}/permissions/{permissionId}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
      - {name: permissionId, in: path, required: true, schema: {type: integer}}
    delete:
      tags: [Roles]
      summary: Remove permission from role
      description: Remove a permission from the specified role, revoking it for all users and groups holding that role.
      operationId: removePermissionFromRole
      security: [{bearerAuth: []}]
      responses:
        '204': {description: Permission removed from role.}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/permissions:
    get:
      tags: [Permissions]
      summary: List permissions
      description: Return all available permissions, optionally filtered by resource type.
      operationId: listPermissions
      security: [{bearerAuth: []}]
      parameters:
        - {name: resource, in: query, description: "Optional filter by resource.", schema: {type: string}}
      responses:
        '200':
          description: "Envelope `data` has `permissions[]` (id, name, resource, ...) and `total`."
        '401': {$ref: '#/components/responses/Error'}

  /api/v1/permissions/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Permissions]
      summary: Get a single permission by ID
      description: Return a single permission by ID, including its resource, action, and description.
      operationId: getPermission
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is the permission (id, name, description, resource, action)."
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/user-roles:
    post:
      tags: [UserRoles]
      summary: Assign role to user at a scope
      description: Assign a role to a user at the specified project or global scope, with an optional JIT expiry.
      operationId: assignUserRole
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, role_id]
              properties:
                user_id: {type: integer}
                role_id: {type: integer}
                project_id: {type: integer, description: "Scope (0 = global)."}
                environment_id: {type: integer, description: "Scope (0 = global)."}
                expires_at: {type: string, format: date-time, nullable: true, description: "RFC3339; omit or null for a non-expiring grant. JIT access — the assignment is automatically revoked when the timestamp is reached."}
      responses:
        '201':
          description: "Envelope `data` is `{user_id, role_id}`."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      user_id: {type: integer}
                      role_id: {type: integer}
                      expires_at: {type: string, format: date-time, nullable: true}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '409': {$ref: '#/components/responses/Error'}
    delete:
      tags: [UserRoles]
      summary: Remove role from user at a scope
      description: Remove a role assignment from a user at the specified project or global scope.
      operationId: removeUserRole
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, role_id]
              properties:
                user_id: {type: integer}
                role_id: {type: integer}
                project_id: {type: integer, description: "Scope (0 = global)."}
                environment_id: {type: integer, description: "Scope (0 = global)."}
      responses:
        '204': {description: Role removed from user.}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/user-roles/user/{userId}:
    parameters:
      - {name: userId, in: path, required: true, schema: {type: integer}}
    get:
      tags: [UserRoles]
      summary: Get a user's role assignment
      description: Return the current role assignment for the specified user.
      operationId: getUserRoleAssignment
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope `data` is the user's role assignment."
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/audit/logs:
    get:
      tags: [Audit]
      summary: List audit log entries (UI-oriented, paginated)
      description: Return paginated audit log entries filtered by action, user, project, actor type, or time range.
      operationId: listAuditLogs
      security: [{bearerAuth: []}]
      parameters:
        - {name: action, in: query, schema: {type: string}, description: Filter by event type/action.}
        - {name: user_id, in: query, schema: {type: integer}, description: Filter by acting user ID.}
        - {name: project_id, in: query, schema: {type: integer}, description: Filter by project ID.}
        - {name: actor_type, in: query, schema: {type: string, enum: [user, machine_identity, system]}, description: Segment human vs machine activity (ADR-023).}
        - {name: start_time, in: query, schema: {type: string, format: date-time}, description: RFC3339 lower bound (inclusive).}
        - {name: end_time, in: query, schema: {type: string, format: date-time}, description: RFC3339 upper bound (inclusive).}
        - {name: page, in: query, schema: {type: integer, default: 1, minimum: 1}}
        - {name: page_size, in: query, schema: {type: integer, default: 50, maximum: 100}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data has logs[] (each: id, event_type, actor,
            actor_type, description, timestamp, optional diff, and impersonation /
            impersonated_by / acting_as when impersonated), plus page, page_size,
            total, total_pages.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/audit/export:
    get:
      tags: [Audit]
      summary: Cursor-paginated full-fidelity audit feed for SIEM pull
      description: Return a cursor-paginated, full-fidelity audit event feed designed for SIEM ingestion.
      operationId: exportAuditLogs
      security: [{bearerAuth: []}]
      parameters:
        - {name: after_id, in: query, schema: {type: integer}, description: Cursor — pass the last returned event id to fetch the next page.}
        - {name: since, in: query, schema: {type: string, format: date-time}, description: RFC3339 lower bound on event time.}
        - {name: limit, in: query, schema: {type: integer, default: 100, maximum: 1000}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data has events[] (each: id, event_type, timestamp,
            actor, user_id, project_id, secret_id, description, ip_address, actor_type,
            success, optional diff/impersonation/impersonated_by/acting_as), count, and
            next_cursor (the id for the next after_id, or null when caught up).
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/audit/export.csv:
    get:
      tags: [Audit]
      summary: Download audit events as CSV (compliance hand-off)
      description: >-
        Returns a bounded, human/auditor-friendly CSV attachment with a header
        row. Distinct from the JSON /audit/export SIEM feed — this is a
        single one-shot download capped at 10 000 rows. Gated by audit.read.
      operationId: exportAuditLogsCSV
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}, description: Filter by project ID.}
        - {name: user_id, in: query, schema: {type: integer}, description: Filter by acting user ID.}
        - {name: since, in: query, schema: {type: string, format: date-time}, description: RFC3339 lower bound on event time (inclusive).}
        - {name: until, in: query, schema: {type: string, format: date-time}, description: RFC3339 upper bound on event time (inclusive).}
        - {name: limit, in: query, schema: {type: integer, default: 1000, maximum: 10000}, description: Maximum rows to include.}
      responses:
        '200':
          description: CSV file download (`text/csv`). Columns — id, event_type, actor, actor_type, description, ip_address, project_id, secret_id, success, timestamp.
          content:
            text/csv:
              schema: {type: string, format: binary}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/audit/search:
    get:
      tags: [Audit]
      summary: Rich-filter audit search
      description: >-
        Exposes finer-grained search filters than GET /audit/logs: partial
        actor username match, exact IP address, resource type/ID, success
        flag, RFC3339 time range, and limit/offset pagination. Gated by
        audit.read.
      operationId: searchAuditLogs
      security: [{bearerAuth: []}]
      parameters:
        - {name: actor, in: query, schema: {type: string}, description: Partial username match on the acting principal.}
        - {name: user_id, in: query, schema: {type: integer}, description: Exact numeric user ID.}
        - {name: project_id, in: query, schema: {type: integer}, description: Exact project ID.}
        - {name: action, in: query, schema: {type: string}, description: "Exact event_type (e.g. secret.read)."}
        - {name: resource_type, in: query, schema: {type: string}, description: "Resource kind prefix (e.g. secret)."}
        - {name: resource_id, in: query, schema: {type: integer}, description: Exact secret_node_id.}
        - {name: ip, in: query, schema: {type: string}, description: Exact originating IP address.}
        - {name: success, in: query, schema: {type: string, enum: [true, false]}, description: Filter by operation success/failure.}
        - {name: since, in: query, schema: {type: string, format: date-time}, description: RFC3339 start of time range (inclusive).}
        - {name: until, in: query, schema: {type: string, format: date-time}, description: RFC3339 end of time range (inclusive).}
        - {name: limit, in: query, schema: {type: integer, default: 100, maximum: 1000}, description: Maximum results.}
        - {name: offset, in: query, schema: {type: integer, default: 0, minimum: 0}, description: Pagination offset.}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data has events[] -- the same shape GET
            /audit/logs returns (each: id, event_type, actor, actor_type,
            description, timestamp, optional diff, and impersonation /
            impersonated_by / acting_as when impersonated; never ip_address or
            the tamper-evidence hash chain) -- and total.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/audit/rbac-logs:
    get:
      tags: [Audit]
      summary: List the role-assignment audit trail
      description: Return paginated records of role, permission, and group assignment changes for RBAC auditing.
      operationId: listRBACAuditLogs
      security: [{bearerAuth: []}]
      parameters:
        - {name: page, in: query, schema: {type: integer, default: 1, minimum: 1}}
        - {name: page_size, in: query, schema: {type: integer, default: 50, maximum: 100}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data has logs[] (each: id, action, actor_user_id,
            target_user_id, group_id, role_id, permission_id, project_id, details,
            created_at), plus page, page_size, total, total_pages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      logs: {type: array, items: {$ref: '#/components/schemas/RBACAuditLogEntry'}}
                      total: {type: integer}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/rbac/permission-matrix:
    get:
      tags: [Roles]
      summary: Export the deployment-wide RBAC permission matrix
      description: >-
        Return every (user, role, permission, scope) tuple in the deployment, for
        SOC2/ISO27001-style access reviews (`keyorix rbac export-matrix`). Relocated
        out of the /system proxy tier to /api/v1/rbac/permission-matrix (#G79);
        added to this spec for ADR-108 Phase 3 PR 3 -- it had no OpenAPI entry at all
        before, despite being a real, routed, roles.read-gated endpoint.
      operationId: getPermissionMatrix
      security: [{bearerAuth: []}]
      parameters:
        - {name: project_id, in: query, schema: {type: integer}, description: "Filter to one project (0/omit = every project, including global grants)."}
        - {name: format, in: query, schema: {type: string, enum: [csv]}, description: "When 'csv', return text/csv instead of the JSON envelope."}
      responses:
        '200':
          description: "Envelope `data` is `{rows, total}` (JSON mode) or a CSV attachment (format=csv)."
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      rows: {type: array, nullable: true, items: {$ref: '#/components/schemas/PermissionMatrixRow'}, description: "nil (JSON null), not [], when there are no grants to report."}
                      total: {type: integer}
            text/csv:
              schema: {type: string, format: binary}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/audit/retention:
    get:
      tags: [Audit]
      summary: Report audit-trail retention coverage (ADR-029)
      description: Return audit-trail retention statistics including oldest event, coverage span, and NIS2 compliance status.
      operationId: getAuditRetention
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: retention_policy (always "unlimited"),
            total_events, oldest_event, newest_event (nullable timestamps), coverage_days,
            and meets_nis2_12_month (true once the trail spans the NIS2 12-month window).
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/audit/verify:
    get:
      tags: [Audit]
      summary: Verify the tamper-evidence hash chain (ADR-029)
      description: Verify the audit log's tamper-evidence hash chain and report the first broken link if any.
      operationId: verifyAuditChain
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: valid, chained_events, unchained_events; on
            failure (valid=false) also first_broken_id and reason naming the first
            divergent event.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/audit/checkpoint:
    post:
      tags: [Audit]
      summary: Write a signed audit-chain checkpoint on demand (ADR-029)
      description: >-
        Signs a checkpoint of the current verified chain head for on-box truncation
        detection. Requires system.write (above the audit group's audit.read) and
        server-side encryption (the signing key is DEK-derived). Use to re-baseline
        immediately after a DEK rotation.
      operationId: writeAuditCheckpoint
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: {id, chained_events, head_id, head_hash,
            key_version} of the new checkpoint.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '409':
          description: The chain does not verify (broken, or a signed checkpoint proves a truncation); refused.
        '412':
          description: Signed checkpoints require encryption to be enabled.

  /api/v1/audit/migrate-chain-encoding:
    post:
      tags: [Audit]
      summary: One-time re-derivation of the audit hash chain under the current encoding (ADR-108 PR 7 addition)
      description: >-
        Re-derives entry_hash/prev_hash for every chained audit_events row under the
        server's current hash encoding, so a breaking hash-format change doesn't make
        `audit verify` report the chain broken starting at the first pre-upgrade row.
        dry_run=true (default) only previews; dry_run=false applies. Same privilege bar
        as /audit/checkpoint (system.write) -- it rewrites the tamper-evidence dataset
        itself. Added for ADR-108 PR 7 (thin CLI `audit migrate-chain-encoding`) -- was
        already a live route (auditHandler.MigrateAuditChainEncoding, router.go), just
        undocumented in this spec until now.
      operationId: migrateAuditChainEncoding
      security: [{bearerAuth: []}]
      parameters:
        - name: dry_run
          in: query
          description: "false applies the migration for real; true (default) only previews."
          schema: {type: string, enum: ['true', 'false']}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: {dry_run, rows_migrated, unchained_rows_skipped,
            head_id, head_hash, anchor_row_id, anchor_new_entry_hash}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/audit/anomalies:
    get:
      tags: [Audit]
      summary: List anomaly alerts
      description: Return secret-access anomaly alerts, optionally filtered by acknowledged state.
      operationId: listAnomalyAlerts
      security: [{bearerAuth: []}]
      parameters:
        - {name: acknowledged, in: query, schema: {type: boolean}, description: Filter by acknowledged state; omit to return all.}
        - {name: unacknowledged, in: query, schema: {type: boolean}, description: Legacy alias (CLI) — true returns only unacknowledged alerts.}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: alerts[] (each: ID, SecretNodeID, SecretName,
            AlertType (off_hours|new_ip|frequency_spike|new_user), Severity (low|medium|high),
            Description, AccessedBy, IPAddress, DetectedAt, Acknowledged, CreatedAt) and total.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/audit/anomalies/{id}/acknowledge:
    post:
      tags: [Audit]
      summary: Acknowledge an anomaly alert
      description: Mark an anomaly alert as acknowledged, indicating it has been reviewed and triaged.
      operationId: acknowledgeAnomalyAlert
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}, description: Anomaly alert ID.}
      responses:
        '200':
          description: 'Envelope {data, message}. data: {acknowledged: true}.'
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/admin/anomaly-config:
    get:
      tags: [Audit]
      summary: Get the runtime anomaly-detection configuration (ADR-108 PR 7 addition)
      description: >-
        Read the DB-persisted anomaly-detection thresholds (lookback window, quarantine
        period, off-hours band, ML Isolation Forest settings). Requires system.read.
        Added for ADR-108 PR 7 (thin CLI `anomalies config get`) -- was already a live
        route (handlers.GetAnomalyConfig, router.go), just undocumented in this spec
        until now.
      operationId: getAnomalyConfig
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data.config: {lookback_days, quarantine_hours,
            off_hours_enabled, off_hours_timezone, off_hours_start, off_hours_end,
            ml_enabled, ml_threshold, ml_num_trees, ml_sample_size, updated_at, updated_by}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    put:
      tags: [Audit]
      summary: Update the runtime anomaly-detection configuration (ADR-108 PR 7 addition)
      description: >-
        Full-row replace of the anomaly-detection configuration (no version/ETag column
        -- see internal/core/anomaly_config.go). Requires system.write. Added for
        ADR-108 PR 7 (thin CLI `anomalies config set`) -- was already a live route
        (handlers.UpdateAnomalyConfig, router.go), just undocumented in this spec until
        now.
      operationId: updateAnomalyConfig
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                lookback_days: {type: integer}
                quarantine_hours: {type: integer}
                off_hours_enabled: {type: boolean}
                off_hours_timezone: {type: string}
                off_hours_start: {type: integer}
                off_hours_end: {type: integer}
                ml_enabled: {type: boolean}
                ml_threshold: {type: number}
                ml_num_trees: {type: integer}
                ml_sample_size: {type: integer}
      responses:
        '200':
          description: "Envelope {data, message}. data.config: same shape as GET."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/alert-escalation-policies:
    get:
      tags: [Audit]
      summary: List alert escalation policies (ADR-108 PR 7 addition)
      description: >-
        Requires system.write (a router design choice applying to both GETs and
        mutations in this family, not a CLI/REST mismatch). Added for ADR-108 PR 7
        (thin CLI `anomalies escalation list`) -- was already a live route
        (alertEscalationHandler.List, router.go), just undocumented in this spec
        until now.
      operationId: listAlertEscalationPolicies
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data.policies[]: {id, name, min_severity,
            escalate_after_minutes, channel_ids, enabled}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Audit]
      summary: Create an alert escalation policy (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `anomalies escalation
        create`) -- was already a live route (alertEscalationHandler.Create,
        router.go), just undocumented in this spec until now.
      operationId: createAlertEscalationPolicy
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: {type: string}
                min_severity: {type: string, description: "low | medium | high | critical"}
                escalate_after_minutes: {type: integer}
                channel_ids: {type: string, description: "Comma-separated NotificationChannel IDs."}
                enabled: {type: boolean}
      responses:
        '201':
          description: >-
            Envelope {data, message}. data: {id, name, min_severity,
            escalate_after_minutes, channel_ids, enabled}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/alert-escalation-policies/{id}:
    delete:
      tags: [Audit]
      summary: Delete an alert escalation policy (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `anomalies escalation
        delete`) -- was already a live route (alertEscalationHandler.Delete,
        router.go), just undocumented in this spec until now.
      operationId: deleteAlertEscalationPolicy
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200':
          description: "Envelope {data=null, message='deleted'}."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/admin/jobs/run-alert-escalation:
    post:
      tags: [Audit]
      summary: Trigger the alert-escalation job immediately (ADR-108 PR 7 addition)
      description: >-
        Evaluates unacknowledged anomaly alerts against every enabled escalation
        policy and escalates the ones past their after-minutes threshold. Requires
        system.write. Added for ADR-108 PR 7 (thin CLI `anomalies escalation run`) --
        was already a live route (alertEscalationHandler.RunEscalation, router.go),
        just undocumented in this spec until now. The old CLI's remote branch called
        the doomed `/system` proxy equivalent instead
        (`/api/v1/system/admin/jobs/run-alert-escalation`) -- the thin CLI uses this
        real, permission-gated, audited human route directly.
      operationId: runAlertEscalation
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope {data, message}. data: {evaluated, escalated, skipped}."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/notification-channels:
    get:
      tags: [Notifications]
      summary: List notification channel destinations (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `notification channel
        list`) -- was already a live route (notificationChannelHandler.List,
        router.go), just undocumented in this spec until now.
      operationId: listNotificationChannels
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data.channels[]: {id, name, type, enabled, url,
            email, events, created_by}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Notifications]
      summary: Add a notification channel (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `notification channel
        add`) -- was already a live route (notificationChannelHandler.Create,
        router.go), just undocumented in this spec until now.
      operationId: createNotificationChannel
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, type]
              properties:
                name: {type: string}
                type: {type: string, description: "webhook | slack | teams | email"}
                enabled: {type: boolean}
                url: {type: string}
                email: {type: string}
                events: {type: string, description: "Comma-separated event types; empty = all."}
      responses:
        '201':
          description: "Envelope {data, message}. data: same shape as list's channels[] entry."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/notification-channels/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: integer}}
    get:
      tags: [Notifications]
      summary: Get a notification channel by ID (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `notification channel
        get`, which resolves a name via list then calls this) -- was already a live
        route (notificationChannelHandler.Get, router.go), just undocumented in this
        spec until now.
      operationId: getNotificationChannel
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope {data, message}. data: same shape as list's channels[] entry."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    put:
      tags: [Notifications]
      summary: Update a notification channel (ADR-108 PR 7 addition)
      description: >-
        Partial update -- only fields present in the body change. Requires
        system.write. Added for ADR-108 PR 7 (thin CLI `notification channel update`)
        -- was already a live route (notificationChannelHandler.Update, router.go),
        just undocumented in this spec until now.
      operationId: updateNotificationChannel
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                url: {type: string}
                email: {type: string}
                events: {type: string}
                enabled: {type: boolean}
      responses:
        '200':
          description: "Envelope {data, message}. data: same shape as list's channels[] entry."
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Notifications]
      summary: Delete a notification channel (ADR-108 PR 7 addition)
      description: >-
        Requires system.write. Added for ADR-108 PR 7 (thin CLI `notification channel
        delete`) -- was already a live route (notificationChannelHandler.Delete,
        router.go), just undocumented in this spec until now.
      operationId: deleteNotificationChannel
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: "Envelope {data=null, message='deleted'}."
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/notifications:
    get:
      tags: [Notifications]
      summary: List the authenticated user's notifications
      description: Return in-app notifications for the authenticated user, optionally filtered to unread only.
      operationId: listNotifications
      security: [{bearerAuth: []}]
      parameters:
        - {name: unread, in: query, schema: {type: boolean}, description: true returns only unread notifications.}
        - {name: limit, in: query, schema: {type: integer}, description: Max notifications to return (0/absent = no limit).}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: notifications[] (each: id, type, title, message,
            link, is_read, created_at, optional project_id), newest first, plus unread_count
            for the bell badge.
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/notifications/read-all:
    post:
      tags: [Notifications]
      summary: Mark all of the user's notifications read
      description: Mark all in-app notifications for the authenticated user as read in one operation.
      operationId: markAllNotificationsRead
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: 'Envelope {data, message}. data: {all_read: true}.'
        '401': {$ref: '#/components/responses/Error'}
  /api/v1/notifications/{id}/read:
    post:
      tags: [Notifications]
      summary: Mark a single notification read
      description: Mark a specific notification as read by its ID; the notification must belong to the caller.
      operationId: markNotificationRead
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}, description: Notification ID (must belong to the caller).}
      responses:
        '200':
          description: 'Envelope {data, message}. data: {id, is_read: true}.'
        '401': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/dashboard/stats:
    get:
      tags: [Dashboard]
      summary: Dashboard summary statistics
      description: >-
        The caller's own home dashboard (secret/share counts, expiring secrets).
        Requires the universal system.read baseline (every real principal holds it).
        The deployment-wide aggregate fields (activeUsers, auditEvents30d,
        auditLogins30d, auditSecretReads30d, failedAuthAttempts24h, inactiveUsers) and
        the full recentActivity feed are additionally scoped to audit.read — a
        baseline-only caller gets zeroed aggregates and an activity feed limited to
        their own events.
      operationId: getDashboardStats
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: totalSecrets, sharedSecrets, secretsSharedWithMe,
            activeUsers, auditEvents30d, auditLogins30d, auditSecretReads30d,
            failedAuthAttempts24h, inactiveUsers; optional trend objects
            (totalSecretsTrend/sharedSecretsTrend/sharedWithMeTrend, each {value, isPositive}),
            expiringSecrets[] (id, name, environment, expiresAt, daysLeft, expired), and
            recentActivity[] (id, type, secretName, timestamp, actor).
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/compliance/posture:
    get:
      tags: [Dashboard]
      summary: Deployment controls-posture report (ISO 27001 / SOC 2 / NIS2 / DORA)
      description: >-
        A deployment-wide, on-demand snapshot of the control posture for auditors,
        rolling up: audit-trail integrity (chain verified, chained events,
        checkpointed), access governance (projects, with-open-campaign,
        never-reviewed, projects overdue for recertification (A.5.18 cadence),
        open campaigns + pending items, dormant role grants,
        separation-of-duties violations), rotation hygiene (covered secrets,
        overdue, due-soon), identity
        (active users, with-second-factor + percent), emergency access
        (active/total break-glass activations), data classification (secret
        counts per sensitivity level + unclassified), anomalies (open /
        high-severity access-anomaly alerts), legal hold (active + reason), and
        data retention (configured per-record-type windows in days; 0 = keep
        forever — A.5.33 storage-limitation). Requires audit.read (not the universal
        system_viewer baseline).
      operationId: getCompliancePosture
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data}. data: {generated_at, audit_integrity, access_governance,
            rotation, identity, emergency_access} — see the description for each group's fields.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/compliance/controls:
    get:
      tags: [Dashboard]
      summary: Compliance control matrix (ISO 27001 / SOC 2 / NIS2 / DORA)
      description: >-
        The control matrix: each control Keyorix enforces, mapped to its clause
        references across ISO 27001 Annex A, SOC 2 Trust Services Criteria, NIS2,
        and DORA, with a live status derived from the posture (pass | gap |
        not_configured) and a human-readable detail. Includes a summary tally.
        Requires audit.read (not the universal system_viewer baseline).
      operationId: getComplianceControls
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data}. data: {generated_at, summary {total, pass, gap,
            not_configured}, controls[] {id, name, area, status, detail, frameworks
            {iso_27001[], soc2[], nis2[], dora[]}}}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/controls.csv:
    get:
      tags: [Dashboard]
      summary: Compliance control matrix as CSV (ADR-108 PR 8 addition)
      description: >-
        The same control matrix as GET /compliance/controls, as a raw CSV download
        (not the {data} envelope). Requires audit.read. Added for ADR-108 PR 8 (thin
        CLI `compliance controls --csv`) -- was already a live route
        (dashboardHandler.ExportComplianceControlsCSV, router.go), just undocumented
        in this spec until now.
      operationId: exportComplianceControlsCSV
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Raw text/csv body (not the JSON envelope)."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/digest:
    get:
      tags: [Dashboard]
      summary: On-demand compliance digest (ADR-108 PR 8 addition)
      description: >-
        The same title + body normally broadcast on a schedule to configured
        notification channels. Requires audit.read. Added for ADR-108 PR 8 (thin CLI
        `compliance digest`) -- was already a live route
        (dashboardHandler.GetComplianceDigest, router.go), just undocumented in this
        spec until now.
      operationId: getComplianceDigest
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {title, body}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/digest/send:
    post:
      tags: [Dashboard]
      summary: Broadcast the compliance digest now (ADR-108 PR 8 addition)
      description: >-
        Trigger an immediate broadcast of the compliance digest to configured
        notification channels. Requires audit.read (matches its read sibling above).
        Added for ADR-108 PR 8 (thin CLI `compliance digest --send`) -- was already a
        live route (dashboardHandler.SendComplianceDigest, router.go), just
        undocumented in this spec until now.
      operationId: sendComplianceDigest
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {sent}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/permission-changes:
    get:
      tags: [Dashboard]
      summary: Role grant/revoke events from the audit trail (ADR-108 PR 8 addition)
      description: >-
        Structured permission-change audit: role grants and revokes in a time
        window. Requires audit.read. Added for ADR-108 PR 8 (thin CLI `compliance
        permission-changes`) -- was already a live route
        (dashboardHandler.GetPermissionChangeAudit, router.go), just undocumented in
        this spec until now.
      operationId: getCompliancePermissionChanges
      security: [{bearerAuth: []}]
      parameters:
        - {name: since, in: query, schema: {type: string, format: date-time}, description: "RFC3339 lower bound; defaults to 30 days ago."}
        - {name: until, in: query, schema: {type: string, format: date-time}, description: "RFC3339 upper bound; defaults to now."}
        - {name: limit, in: query, schema: {type: integer}, description: "Max events (default 100, max 1000)."}
      responses:
        '200':
          description: >-
            Envelope {data}. data: {since, until, total, changes[] {event_id, action,
            actor_name, target_user, role_name, scope, changed_at}}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/permission-baseline:
    get:
      tags: [Dashboard]
      summary: Every user's effective permissions (ADR-108 PR 8 addition)
      description: >-
        The full permission baseline, expanded through direct role grants and group
        membership, as JSON. Requires audit.read. Added for ADR-108 PR 8 (thin CLI
        `compliance permission-baseline --format json`) -- was already a live route
        (dashboardHandler.GetPermissionBaseline, router.go), just undocumented in
        this spec until now.
      operationId: getCompliancePermissionBaseline
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: the baseline edge list."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/permission-baseline.csv:
    get:
      tags: [Dashboard]
      summary: Every user's effective permissions as CSV (ADR-108 PR 8 addition)
      description: >-
        The same permission baseline as GET /compliance/permission-baseline, as a raw
        CSV download (not the {data} envelope). Requires audit.read. Added for
        ADR-108 PR 8 (thin CLI `compliance permission-baseline`, its default format)
        -- was already a live route (dashboardHandler.GetPermissionBaselineCSV,
        router.go), just undocumented in this spec until now.
      operationId: getCompliancePermissionBaselineCSV
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Raw text/csv body (not the JSON envelope)."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/credential-trends:
    get:
      tags: [Dashboard]
      summary: 30/60/90-day credential hygiene trends (ADR-108 PR 8 addition)
      description: >-
        Per-day stale/expired PAT and stale machine-credential counts over a
        30/60/90-day window. Requires audit.read. Added for ADR-108 PR 8 (thin CLI
        `compliance credential-trends`) -- was already a live route
        (hygieneTrendsHandler.GetCredentialTrends, router.go), just undocumented in
        this spec until now.
      operationId: getComplianceCredentialTrends
      security: [{bearerAuth: []}]
      parameters:
        - {name: days, in: query, schema: {type: integer, enum: [30, 60, 90], default: 30}}
      responses:
        '200':
          description: >-
            Envelope {data}. data: {days, points[] {date, stale_pats, expired_pats,
            stale_machines, total_pats, total_machines}}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/rotation-by-backend:
    get:
      tags: [Dashboard]
      summary: Rotation-overdue secrets grouped by backend (ADR-108 PR 8 addition)
      description: >-
        A deployment-wide tally of rotation-overdue secrets grouped by
        RotationBackend. Requires audit.read. Added for ADR-108 PR 8 (thin CLI
        `compliance rotation-by-backend`) -- was already a live route
        (secretHandler.RotationByBackend, router.go), just undocumented in this spec
        until now.
      operationId: getComplianceRotationByBackend
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data}. data: {generated_at, total_overdue, backends[] {backend,
            total, overdue, up_to_date, never_rotated}}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/secrets/inventory.csv:
    get:
      tags: [Secrets]
      summary: Deployment-wide secret asset inventory as CSV (ADR-108 PR 8 addition)
      description: >-
        Secrets listed by name, project, environment, type, classification, owner,
        and lifecycle status -- no secret values. Requires audit.read. Added for
        ADR-108 PR 8 (thin CLI `compliance inventory`, deployment-wide) -- was
        already a live route (secretHandler.DeploymentSecretsInventoryCSV,
        router.go), just undocumented in this spec until now.
      operationId: getSecretsInventoryCSV
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Raw text/csv body (not the JSON envelope)."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/projects/{id}/secrets/inventory.csv:
    get:
      tags: [Secrets]
      summary: One project's secret asset inventory as CSV (ADR-108 PR 8 addition)
      description: >-
        Same as GET /secrets/inventory.csv, scoped to one project. Requires
        secrets.read on that project. Added for ADR-108 PR 8 (thin CLI `compliance
        inventory --project`) -- was already a live route
        (secretHandler.SecretsInventoryCSV, router.go), just undocumented in this
        spec until now.
      operationId: getProjectSecretsInventoryCSV
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200': {description: "Raw text/csv body (not the JSON envelope)."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/hygiene:
    get:
      tags: [Secrets]
      summary: Deployment-wide secret-hygiene rollup (ADR-108 PR 8 addition)
      description: >-
        Install-wide totals of every project's cleanup signals plus a per-project
        breakdown of the projects that carry debt. Requires audit.read. Added for
        ADR-108 PR 8 (thin CLI `hygiene`) -- was already a live route
        (secretHandler.DeploymentHygiene, router.go), just undocumented in this spec
        until now.
      operationId: getDeploymentHygiene
      security: [{bearerAuth: []}]
      parameters:
        - {name: unused_days, in: query, schema: {type: integer}, description: "Unused-secret window in days (server default: 90)."}
        - {name: expiring_days, in: query, schema: {type: integer}, description: "Expiring-secret window in days (server default: 30)."}
        - {name: stale_days, in: query, schema: {type: integer}, description: "Stale-machine-identity window in days (server default: 90)."}
      responses:
        '200':
          description: >-
            Envelope {data}. data: {totals {orphaned_secrets, unused_secrets,
            expiring_secrets, stale_machine_identities, rotation_overdue},
            projects[] {project_id, project_name, ...same counts}}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}

  /api/v1/compliance/evidence:
    get:
      tags: [Dashboard]
      summary: Auditor evidence pack (posture + supporting records)
      description: >-
        A timestamped, archivable evidence bundle: the posture plus the records that
        substantiate it — the audit-chain anchor {valid, chained_events, head_id,
        head_hash, checkpointed}, the access-review campaigns (per project, with
        tallies), the break-glass register, and the overdue rotations. Requires
        audit.read (not the universal system_viewer baseline).
      operationId: getComplianceEvidence
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: >-
            Envelope {data}. data: {generated_at, posture, audit_anchor, campaigns[],
            break_glass[], rotation_overdue[], sod_violations[]}.
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/compliance/evidence/verify:
    post:
      tags: [Dashboard]
      summary: Verify an exported evidence pack against its signature
      description: >-
        Recomputes the HMAC over the supplied evidence-pack bytes with the server's
        DEK-derived signing key and compares it to the detached signature — proving an
        archived pack was produced by this deployment and is unmodified. The pack is
        base64-encoded in data_b64 so arbitrary JSON can ride in the envelope. A
        signature made under a superseded DEK version (post key-rotation) is reported
        as unverifiable, distinct from a tamper mismatch. Requires audit.read (not the
        universal system_viewer baseline).
      operationId: verifyComplianceEvidence
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [data_b64, signature]
              properties:
                data_b64:  {type: string, description: "Base64 of the evidence-pack bytes."}
                signature: {type: string, description: "The detached signature ('<keyVersion>:<hmac-hex>')."}
      responses:
        '200':
          description: >-
            Envelope {data}. data: {valid, key_version, current_version, reason}.
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/legal-hold:
    get:
      tags: [Dashboard]
      summary: Legal-hold status (ISO 27001 A.5.34 / eDiscovery)
      description: >-
        Whether a deployment-wide litigation/investigation hold is active. While
        active, the background purge jobs are blocked from hard-deleting records.
        Requires audit.read (not the universal system_viewer baseline).
      operationId: getLegalHold
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {active} or {active:true, hold:{id, reason, placed_by, placed_at}}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Dashboard]
      summary: Place a legal hold (block all purges)
      description: "Activate a hold with a reason; refuses if one is already active. Requires system.write."
      operationId: placeLegalHold
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: {type: string, description: "Why the hold is placed (recorded for audit)"}
      responses:
        '201': {description: "Envelope {data}. data: {hold}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    delete:
      tags: [Dashboard]
      summary: Lift the active legal hold (resume purges)
      description: >-
        Remove the active deployment-wide legal hold, allowing background purge jobs
        to resume. The handler reads a JSON body on this DELETE (unusual for REST,
        but genuinely how the wire protocol works here -- LiftLegalHold requires a
        non-empty reason, mapped to 400 when absent); this requestBody was missing
        from the spec until now (ADR-108 PR 8 addition) even though the route itself
        predates this PR.
      operationId: liftLegalHold
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: {type: string, description: "Why the hold is lifted (recorded for audit)."}
      responses:
        '200': {description: "Envelope {data, message}. Legal hold lifted."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/risk-exceptions:
    get:
      tags: [Dashboard]
      summary: Risk register — governed, time-bound exceptions (ISO 27001 A.5.8)
      description: >-
        The risk register: governed acceptances of a known control gap, each with an
        owner, justification, and sunset date. Active only by default; ?all=true
        includes expired exceptions (history). Each carries a computed status
        (active | expired | revoked). Requires audit.read (not the universal
        system_viewer baseline).
      operationId: listRiskExceptions
      parameters:
        - {name: all, in: query, schema: {type: boolean}, description: "Include expired exceptions."}
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {exceptions[] {id, title, category, reference, justification, status, expires_at, created_by, created_at}}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Dashboard]
      summary: Record a risk exception (accept a control gap for a bounded time)
      description: "Accept a known gap with a justification and a required future expiry. Requires system.write."
      operationId: createRiskException
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, justification, expires_at]
              properties:
                title: {type: string}
                category: {type: string, enum: [sod, mfa, rotation, dormant_access, classification, other]}
                reference: {type: string, description: "What it applies to (a user, an SoD pair, a secret)."}
                justification: {type: string}
                expires_at: {type: string, format: date-time, description: "RFC3339; must be in the future."}
      responses:
        '201': {description: "Envelope {data, message}. data: {exception}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/risk-exceptions/{id}/approve:
    post:
      tags: [Dashboard]
      summary: Approve a risk exception so it takes effect (ADR-108 PR 8 addition)
      description: >-
        Dual control: an exception only suppresses its matched violation from the
        compliance posture once a DIFFERENT system.write holder than its creator
        approves it. Requires system.write. Added for ADR-108 PR 8 (thin CLI `risk
        approve`) -- was already a live route (dashboardHandler.ApproveRiskException,
        router.go), just undocumented in this spec until now.
      operationId: approveRiskException
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      responses:
        '200': {description: "Envelope {data, message}. Risk exception approved."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}

  /api/v1/risk-exceptions/{id}:
    delete:
      tags: [Dashboard]
      summary: Revoke a risk exception before its expiry (ISO 27001 A.5.8)
      description: Revoke a risk exception before its expiry date, immediately closing the accepted control gap.
      operationId: revokeRiskException
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer}}
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data, message}. Risk exception revoked."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/sod/policies:
    get:
      tags: [Dashboard]
      summary: List separation-of-duties policies (ISO 27001 A.5.3)
      description: "Toxic permission combinations one principal must not hold together. Requires system.read."
      operationId: listSoDPolicies
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {policies[] of {id, name, description, permission_a, permission_b, created_at}, count}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
    post:
      tags: [Dashboard]
      summary: Create a separation-of-duties policy
      description: "Define two permissions that must not be held together. Requires system.write."
      operationId: createSoDPolicy
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, permission_a, permission_b]
              properties:
                name: {type: string}
                description: {type: string}
                permission_a: {type: string, description: "e.g. roles.assign"}
                permission_b: {type: string, description: "e.g. secrets.delete (must differ from permission_a)"}
      responses:
        '201': {description: "Envelope {data}. data: {policy}."}
        '400': {$ref: '#/components/responses/Error'}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/sod/policies/{id}:
    delete:
      tags: [Dashboard]
      summary: Delete a separation-of-duties policy
      description: Permanently delete a separation-of-duties policy, removing the toxic-pair constraint.
      operationId: deleteSoDPolicy
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: integer, format: uint32}}
      responses:
        '200': {description: "Envelope {data, message}. Policy deleted."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
        '404': {$ref: '#/components/responses/Error'}
  /api/v1/sod/violations:
    get:
      tags: [Dashboard]
      summary: List separation-of-duties violations
      description: >-
        Principals whose effective permissions include both sides of a policy —
        discloses violator names/emails deployment-wide. Requires audit.read (not the
        universal system_viewer baseline).
      operationId: listSoDViolations
      security: [{bearerAuth: []}]
      responses:
        '200': {description: "Envelope {data}. data: {violations[] of {policy_id, policy_name, user_id, username, email, permission_a, permission_b}, count}."}
        '401': {$ref: '#/components/responses/Error'}
        '403': {$ref: '#/components/responses/Error'}
  /api/v1/dashboard/activity:
    get:
      tags: [Dashboard]
      summary: Paginated activity feed
      description: Return a paginated activity feed of recent secret operations for the dashboard.
      operationId: getDashboardActivity
      security: [{bearerAuth: []}]
      parameters:
        - {name: page, in: query, schema: {type: integer, default: 1, minimum: 1}}
        - {name: pageSize, in: query, schema: {type: integer, default: 10, maximum: 100}}
      responses:
        '200':
          description: >-
            Envelope {data, message}. data: items[] (each: id, type, secretName, timestamp,
            actor), total, page, pageSize.
        '401': {$ref: '#/components/responses/Error'}
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: |
        Session token, personal access token (kx_pat_…), or machine token (kx_machine_…).
  responses:
    Error:
      description: >-
        Error response, matching server/http/handlers/helpers.go's sendError verbatim.
        `code` and `message` are top-level, and `error` is a short string label (e.g.
        "NotFound"), NOT a nested object -- confirmed 2026-09-24 against a live server
        (docs/cli-split-inventory.md PR 2's live-verification step) after this schema's
        previous shape (a nested `error: {code, message, type}` object with no `code`/
        `message` at the top level) was found to have never matched any real response
        this server has ever sent -- it broke the first oapi-codegen-generated client to
        actually parse an error path (the thin CLI), which fails loudly on a schema
        mismatch rather than silently misreading a field, unlike a raw-JSON caller.
      content:
        application/json:
          schema:
            type: object
            properties:
              success: {type: boolean}
              code: {type: integer}
              error: {type: string, description: 'Short error-type label, e.g. "NotFound", "BadRequest".'}
              message: {type: string}
              details: {type: object, additionalProperties: true, nullable: true}
  schemas:
    # docs/cli-split-inventory.md §7 PR 9 -- share.
    # models.ShareRecord carries no `json:` tags, so it serializes with bare
    # (capitalized) Go field names -- see the identical note on PR 3/4/5's schemas
    # for this same pattern on other untagged storage models.
    Share:
      type: object
      description: >-
        A secret-share grant. PascalCase field names because models.ShareRecord
        has no json struct tags.
      properties:
        ID: {type: integer}
        SecretID: {type: integer}
        OwnerID: {type: integer}
        RecipientID: {type: integer}
        IsGroup: {type: boolean}
        Permission: {type: string, enum: [read, write]}
        ExpiresAt: {type: string, format: date-time, nullable: true}
        CreatedAt: {type: string, format: date-time}
        UpdatedAt: {type: string, format: date-time}
    # docs/cli-split-inventory.md §7 PR 5 -- secret bulk/rotation/export/import/scan/hygiene.
    # models.SecretNode carries no `json:` tags, so it serializes with bare (capitalized)
    # Go field names -- see the identical note on PR 3/PR 4's schemas for this same model.
    Secret:
      type: object
      description: >-
        A secret or folder node (internal/storage/models.SecretNode), metadata
        only -- never a value. No `json:` tags on the model -- wire keys are the
        bare Go field names (ID, ProjectID, ...), not snake_case (ADR-108 PR 4/5).
      properties:
        ID: {type: integer}
        ParentID: {type: integer, nullable: true}
        ProjectID: {type: integer}
        EnvironmentID: {type: integer}
        Name: {type: string}
        IsSecret: {type: boolean, description: "false = this node is a folder."}
        Type: {type: string}
        Description: {type: string}
        MaxReads: {type: integer, nullable: true}
        ReadCount: {type: integer}
        Expiration: {type: string, format: date-time, nullable: true}
        Classification: {type: string}
        Status: {type: string}
        CreatedBy: {type: string}
        OwnerID: {type: integer}
        OwnerMachineIdentityID: {type: integer}
        IsShared: {type: boolean}
        CreatedAt: {type: string, format: date-time}
        UpdatedAt: {type: string, format: date-time}
        LastRotatedAt: {type: string, format: date-time, nullable: true}
        AutoRotate: {type: boolean}
        RotationLength: {type: integer}
        RotationCharset: {type: string}
        RotationBackend: {type: string}
        RotationRef: {type: string}
    RotationDryRunCheck:
      type: object
      properties:
        name: {type: string}
        passed: {type: boolean}
        message: {type: string}
    RotationDryRunResult:
      type: object
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        backend: {type: string}
        ref: {type: string}
        valid: {type: boolean}
        checks: {type: array, items: {$ref: '#/components/schemas/RotationDryRunCheck'}}
        simulated_at: {type: string, format: date-time}
    SecretAuditEntry:
      type: object
      description: One lifecycle event of a secret. Never carries a plaintext value.
      properties:
        id: {type: integer}
        event_type: {type: string}
        timestamp: {type: string}
        user_id: {type: integer, nullable: true}
        actor_type: {type: string}
        description: {type: string}
        success: {type: boolean}
        diff: {type: object, nullable: true}
        impersonation: {type: boolean}
    OwnershipRecord:
      type: object
      properties:
        event_id: {type: integer}
        from_id: {type: integer}
        to_id: {type: integer}
        changed_by: {type: integer}
        changed_at: {type: string, format: date-time}
        description: {type: string}
    CertificateInfo:
      type: object
      description: Public X.509 metadata of a certificate-valued secret. Never the value or key.
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        subject: {type: string}
        issuer: {type: string}
        serial_number: {type: string}
        not_before: {type: string, format: date-time}
        not_after: {type: string, format: date-time}
        days_until_expiry: {type: integer}
        is_expired: {type: boolean}
        is_ca: {type: boolean}
        self_signed: {type: boolean}
        dns_names: {type: array, items: {type: string}}
        signature_algorithm: {type: string}
        public_key_algorithm: {type: string}
    BlastRadiusNode:
      type: object
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        project_id: {type: integer}
        owner_id: {type: integer}
        depth: {type: integer}
        risk_level: {type: string, enum: [critical, high, medium, low]}
    BlastRadiusReport:
      type: object
      properties:
        source_secret_id: {type: integer}
        source_secret_name: {type: string}
        dependents: {type: array, items: {$ref: '#/components/schemas/BlastRadiusNode'}}
        total_impact: {type: integer}
        max_depth: {type: integer}
        truncated: {type: boolean}
    ExpiringSecretEntry:
      type: object
      properties:
        id: {type: integer}
        name: {type: string}
        type: {type: string}
        classification: {type: string}
        environment_id: {type: integer}
        expiration: {type: string, format: date-time}
        expired: {type: boolean}
    ExpiringSecretsReport:
      type: object
      properties:
        expiring: {type: array, items: {$ref: '#/components/schemas/ExpiringSecretEntry'}}
        total: {type: integer}
        truncated: {type: boolean}
    OrphanedSecretEntry:
      type: object
      description: A secret whose owner is no longer a live user. Metadata only.
      properties:
        id: {type: integer}
        name: {type: string}
        type: {type: string}
        classification: {type: string}
        environment_id: {type: integer}
        owner_id: {type: integer}
    OrphanedSecretsReport:
      type: object
      properties:
        orphaned: {type: array, items: {$ref: '#/components/schemas/OrphanedSecretEntry'}}
        total: {type: integer}
    SecretNameViolation:
      type: object
      properties:
        id: {type: integer}
        name: {type: string}
        type: {type: string}
        classification: {type: string}
        environment_id: {type: integer}
        reason: {type: string}
    SecretNameConformanceReport:
      type: object
      properties:
        policy_enabled: {type: boolean}
        pattern: {type: string}
        max_length: {type: integer}
        total_secrets: {type: integer}
        violations: {type: array, items: {$ref: '#/components/schemas/SecretNameViolation'}}
        truncated: {type: boolean}
    DeploymentSecretNameViolation:
      type: object
      description: A SecretNameViolation tagged with its project (org-wide report).
      properties:
        project_id: {type: integer}
        project_name: {type: string}
        id: {type: integer}
        name: {type: string}
        type: {type: string}
        classification: {type: string}
        environment_id: {type: integer}
        reason: {type: string}
    DeploymentSecretNameConformanceReport:
      type: object
      properties:
        policy_enabled: {type: boolean}
        pattern: {type: string}
        max_length: {type: integer}
        total_secrets: {type: integer}
        truncated: {type: boolean}
        violations: {type: array, items: {$ref: '#/components/schemas/DeploymentSecretNameViolation'}}
    QuotaReportRow:
      type: object
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        read_count: {type: integer}
        max_reads: {type: integer}
        usage_pct: {type: integer}
        status: {type: string, enum: [ok, warning, critical, exhausted]}
    ProjectUsageStat:
      type: object
      description: One project's row in a usage report, aggregating metrics over the reporting window.
      properties:
        project_id: {type: integer}
        project_name: {type: string}
        secret_count: {type: integer}
        reads_in_window: {type: integer}
        unique_readers: {type: integer}
    UsageReport:
      type: object
      properties:
        window_days: {type: integer}
        generated_at: {type: string, format: date-time}
        projects: {type: array, items: {$ref: '#/components/schemas/ProjectUsageStat'}}
    BillingProjectStat:
      type: object
      description: One project's usage breakdown for a billing period.
      properties:
        project_id: {type: integer}
        project_name: {type: string}
        secret_count: {type: integer}
        secret_reads: {type: integer}
        secret_writes: {type: integer}
        secret_rotations: {type: integer}
        unique_users: {type: integer}
        machine_reads: {type: integer}
    BillingTotals:
      type: object
      description: BillingProjectStat aggregated across all projects.
      properties:
        projects: {type: integer}
        secret_count: {type: integer}
        secret_reads: {type: integer}
        secret_writes: {type: integer}
        secret_rotations: {type: integer}
        unique_users: {type: integer}
        machine_reads: {type: integer}
    BillingReport:
      type: object
      properties:
        from: {type: string, format: date-time}
        to: {type: string, format: date-time}
        generated_at: {type: string, format: date-time}
        projects: {type: array, items: {$ref: '#/components/schemas/BillingProjectStat'}}
        totals: {$ref: '#/components/schemas/BillingTotals'}
    BulkOpError:
      type: object
      properties:
        secret_id: {type: integer}
        name: {type: string}
        error: {type: string}
    BulkDeleteResult:
      type: object
      properties:
        deleted: {type: array, items: {type: integer}}
        failed: {type: array, items: {$ref: '#/components/schemas/BulkOpError'}}
        total: {type: integer}
    BulkRotateResult:
      type: object
      properties:
        triggered: {type: array, items: {type: integer}}
        failed: {type: array, items: {$ref: '#/components/schemas/BulkOpError'}}
        total: {type: integer}
    SecretRenameOutcome:
      type: object
      properties:
        id: {type: integer}
        old_name: {type: string}
        new_name: {type: string}
        status: {type: string}
        reason: {type: string}
    BulkRenameReport:
      type: object
      properties:
        dry_run: {type: boolean}
        renamed: {type: integer}
        skipped: {type: integer}
        outcomes: {type: array, items: {$ref: '#/components/schemas/SecretRenameOutcome'}}
    SecretRiskFactor:
      type: object
      properties:
        key: {type: string}
        label: {type: string}
        score: {type: integer}
        weight: {type: number}
        detail: {type: string}
    SecretRiskScore:
      type: object
      description: Composite risk score (0-100, higher = riskier) for one secret.
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        score: {type: integer}
        band: {type: string, enum: [low, medium, high]}
        factors: {type: array, items: {$ref: '#/components/schemas/SecretRiskFactor'}}
        degraded: {type: boolean}
    AccessReviewDecision:
      type: object
      description: >-
        Identifies one access-review entry for a recertification decision
        (attest or revoke). Mirrors an entry from GET .../access-review.
      required: [source]
      properties:
        source:
          type: string
          enum: [role, direct_share, group_share]
          description: Which mechanism conferred the grant.
        principal_type:
          type: string
          enum: [user, group]
          description: For a role grant, whether the principal is a user or a group.
        principal_id:
          type: integer
          format: uint32
          description: The user or group ID the grant is for.
        role_id:
          type: integer
          format: uint32
          description: Required when source=role.
        environment_id:
          type: integer
          format: uint32
          description: Environment scope of a role grant (0 = whole project).
        secret_id:
          type: integer
          format: uint32
          description: Required when source=direct_share or group_share.
    SecretACL:
      type: object
      description: >-
        Per-secret ACL grant. Confers the listed permissions on a specific user
        for one secret, independently of project RBAC (RBAC Phase 3).
      properties:
        id: {type: integer}
        secret_id: {type: integer}
        user_id: {type: integer}
        permissions:
          type: array
          items: {type: string}
        granted_by: {type: integer}
        created_at: {type: string, format: date-time}
    PATToken:
      type: object
      description: >-
        A personal access token (ADR-027/ADR-042). The raw secret is never
        returned except once, in the create response.
      properties:
        id: {type: integer}
        name: {type: string}
        token_prefix: {type: string}
        revoked: {type: boolean}
        created_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time, nullable: true}
        last_used_at: {type: string, format: date-time, nullable: true}
        scopes: {type: array, items: {type: string}, nullable: true}
        project_scope: {type: integer}
        environment_scope: {type: integer}
        allowed_cidrs: {type: array, items: {type: string}, nullable: true}
    PATHygieneRow:
      type: object
      description: >-
        A deployment-wide PAT hygiene entry (GET /pat-hygiene) -- never
        includes the token secret.
      properties:
        id: {type: integer}
        name: {type: string}
        token_prefix: {type: string}
        user_id: {type: integer}
        expires_at: {type: string}
        last_used_at: {type: string}
        expired: {type: boolean}
        stale: {type: boolean}
    MachineIdentity:
      type: object
      description: A project-scoped machine identity (ADR-023).
      properties:
        id: {type: integer}
        project_id: {type: integer}
        name: {type: string}
        identity_type: {type: string}
        state: {type: string, enum: [pending, active, suspended, revoked]}
        description: {type: string}
        created_by: {type: integer}
        created_by_machine_identity_id: {type: integer}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        last_seen_at: {type: string, format: date-time, nullable: true}
        revoked_at: {type: string, format: date-time, nullable: true}
        classification: {type: string}
    MachineToken:
      type: object
      description: Machine identity token metadata (never the raw secret, except once at issuance).
      properties:
        id: {type: integer}
        name: {type: string}
        prefix: {type: string}
        last_used_at: {type: string, format: date-time, nullable: true}
        expires_at: {type: string, format: date-time, nullable: true}
        revoked: {type: boolean}
    MachineTokenHygieneRow:
      type: object
      description: >-
        A deployment-wide machine-token hygiene entry (GET /machine-token-hygiene)
        -- never includes the token secret.
      properties:
        id: {type: integer}
        machine_identity_id: {type: integer}
        name: {type: string}
        token_prefix: {type: string}
        last_used_at: {type: string}
        expired: {type: boolean}
        stale: {type: boolean}
    OIDCBinding:
      type: object
      description: >-
        An OIDC federation binding (ADR-031) mapping an external (issuer,
        subject) to a machine identity.
      properties:
        id: {type: integer}
        issuer: {type: string}
        subject: {type: string}
        created_at: {type: string, format: date-time}
    MachineAuditRow:
      type: object
      properties:
        machine_id: {type: integer}
        name: {type: string}
        description: {type: string}
        credential_count: {type: integer}
        # Not nullable: internal/core.MachineAuditRow tags this `,omitempty` and OMITS
        # the key entirely when nil (never sends an explicit null) -- `nullable: true`
        # here would make the generated Go field lose its own omitempty, so re-encoding
        # it (as `machine audit`'s JSON output does) would print `"last_used_at": null`
        # where the old CLI's hand-written struct prints nothing. Found live
        # (docs/cli-split-inventory.md PR 2's verification step).
        last_used_at: {type: string, format: date-time}
        is_stale: {type: boolean}
        is_revoked: {type: boolean}
        created_at: {type: string, format: date-time}
    MachineAuditReport:
      type: object
      description: Deployment-wide machine identity audit report (GET /machine-identities/audit).
      properties:
        generated_at: {type: string, format: date-time}
        total_count: {type: integer}
        stale_count: {type: integer}
        revoked_count: {type: integer}
        machines:
          type: array
          items: {$ref: '#/components/schemas/MachineAuditRow'}
    SecretGetResult:
      type: object
      description: >-
        GET /api/v1/secrets/{id}'s response `data`: the bare Secret fields
        directly (metadata-only), OR, when include_value=true, `{secret,
        value}` instead. Declared as one flat schema combining both shapes'
        properties (rather than oneOf) since the CLI always knows in advance
        which branch a given request will get -- it controls include_value.
      properties:
        ID: {type: integer}
        ParentID: {type: integer, nullable: true}
        ProjectID: {type: integer}
        EnvironmentID: {type: integer}
        Name: {type: string}
        IsSecret: {type: boolean}
        Type: {type: string}
        Description: {type: string}
        MaxReads: {type: integer, nullable: true}
        ReadCount: {type: integer}
        Expiration: {type: string, format: date-time, nullable: true}
        Classification: {type: string}
        Status: {type: string}
        CreatedBy: {type: string}
        OwnerID: {type: integer}
        IsShared: {type: boolean}
        CreatedAt: {type: string, format: date-time}
        UpdatedAt: {type: string, format: date-time}
        LastRotatedAt: {type: string, format: date-time, nullable: true}
        secret: {$ref: '#/components/schemas/Secret'}
        value: {type: string, description: "The decrypted value. Present only when include_value=true."}
    SecretListEntry:
      type: object
      description: >-
        One row of GET /api/v1/secrets's data.secrets[]
        (internal/storage/models.SecretWithSharingInfo, which anonymously
        embeds *SecretNode -- Go's encoding/json promotes the embedded
        struct's fields onto this same object). Wire keys are a genuine MIX
        of case conventions: SecretNode's own fields are bare capitalized
        (ID, Name, IsShared, ...), while SecretWithSharingInfo's own fields
        are snake_case (project_name, is_shared, ...) -- IsShared and
        is_shared are both present as DIFFERENT keys on the same object,
        from the two different structs.
      properties:
        ID: {type: integer}
        ParentID: {type: integer, nullable: true}
        ProjectID: {type: integer}
        EnvironmentID: {type: integer}
        Name: {type: string}
        IsSecret: {type: boolean}
        Type: {type: string}
        Description: {type: string}
        MaxReads: {type: integer, nullable: true}
        ReadCount: {type: integer}
        Expiration: {type: string, format: date-time, nullable: true}
        Classification: {type: string}
        Status: {type: string}
        CreatedBy: {type: string}
        OwnerID: {type: integer}
        IsShared: {type: boolean, description: "From the embedded SecretNode. SecretWithSharingInfo's own is_shared field also exists on the wire, distinct from this one -- omitted here (oapi-codegen mangles snake_case to the same Go field name, IsShared, as this property, causing a real generation collision) since no CLI command reads it."}
        CreatedAt: {type: string, format: date-time}
        UpdatedAt: {type: string, format: date-time}
        project_name: {type: string}
        environment_name: {type: string}
        is_owned_by_user: {type: boolean}
        owner_username: {type: string}
        user_permission: {type: string}
        share_count: {type: integer}
    SecretVersion:
      type: object
      description: >-
        A secret version's metadata (internal/storage/models.SecretVersion).
        No `json:` tags except EncryptedValue/EncryptionMetadata (both
        `json:"-"`, never sent) -- wire keys for the rest are bare
        capitalized Go field names.
      properties:
        ID: {type: integer}
        SecretNodeID: {type: integer}
        VersionNumber: {type: integer}
        ReadCount: {type: integer}
        CreatedAt: {type: string, format: date-time}
    SecretVersionDiffChange:
      type: object
      properties:
        field: {type: string}
        old_value: {type: string}
        new_value: {type: string}
    SecretVersionDiffResult:
      type: object
      properties:
        secret_name: {type: string}
        from_version: {type: integer}
        to_version: {type: integer}
        changes:
          type: array
          items: {$ref: '#/components/schemas/SecretVersionDiffChange'}
          nullable: true
          description: >-
            Null (not an empty array) when nothing tracked (classification,
            expiry, read count) differs between the two versions -- values
            themselves are never compared or included here.
        acl_user_ids:
          type: array
          items: {type: integer}
          nullable: true
          description: >-
            Current ACL snapshot. Null (not an empty array) when the secret has
            no ACL grants -- the underlying nil Go slice marshals as null -- or
            when the caller lacks secrets.manage and the handler strips it
            entirely (see degraded below).
        degraded:
          type: boolean
          description: >-
            True when the server's ACL lookup itself failed (#G54) --
            acl_user_ids is then an unreliable, possibly-empty placeholder,
            NOT an authoritative "no ACL grants" answer.
    SecretVersionComment:
      type: object
      properties:
        id: {type: integer}
        comment: {type: string}
        username: {type: string}
        created_at: {type: string, format: date-time}
    SecretDependencyEdge:
      type: object
      properties:
        id: {type: integer}
        secret_id: {type: integer}
        secret_name: {type: string}
        note: {type: string}
    SecretDependencies:
      type: object
      properties:
        secret_id: {type: integer}
        depends_on: {type: array, items: {$ref: '#/components/schemas/SecretDependencyEdge'}}
        dependents: {type: array, items: {$ref: '#/components/schemas/SecretDependencyEdge'}}
    SecretImpactedSecret:
      type: object
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        depth: {type: integer, description: "Hops from the rotated secret in the dependency graph."}
    SecretImpact:
      type: object
      description: The blast radius of rotating a secret -- every transitively-dependent secret.
      properties:
        secret_id: {type: integer}
        secret_name: {type: string}
        affected: {type: array, items: {$ref: '#/components/schemas/SecretImpactedSecret'}}
    SecretAccessor:
      type: object
      description: One entry in a secret's effective access list (owner, direct share, or group share).
      properties:
        username: {type: string}
        permission: {type: string}
        source: {type: string, description: "How access was granted: owner, direct share, or group share."}
    SecretAccessLogEntry:
      type: object
      description: >-
        One read event from a secret's access log (server/http/handlers/secrets_access_history.go's
        secretAccessLogEntry -- a snake_case DTO, not the raw internal/storage/models.SecretAccessLog).
        ip_address/user_agent are present only when the caller separately holds audit.read
        (see the route's own description); an ordinary secrets.read-only caller never
        sees another user's originating IP.
      properties:
        id: {type: integer}
        secret_version_id: {type: integer}
        accessed_by: {type: string}
        action: {type: string}
        access_time: {type: string, format: date-time}
        ip_address: {type: string, description: "Only present for a caller who separately holds audit.read."}
        user_agent: {type: string, description: "Only present for a caller who separately holds audit.read."}
    SecretAccessSchedule:
      type: object
      description: A secret's temporal access-window policy (internal/storage/models.SecretAccessSchedule -- properly snake_case-tagged, unlike SecretNode).
      properties:
        id: {type: integer}
        secret_node_id: {type: integer}
        allowed_days: {type: string, description: "Comma-separated ISO weekday list (1=Mon..7=Sun), or \"*\"."}
        start_hour: {type: integer}
        end_hour: {type: integer}
        timezone: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    SecretTemplate:
      type: object
      description: A reusable secret-creation metadata preset (internal/storage/models.SecretTemplate -- properly snake_case-tagged).
      properties:
        id: {type: integer}
        name: {type: string}
        description: {type: string}
        default_classification: {type: string}
        default_tags: {type: string, description: "Comma-separated tag names."}
        description_pattern: {type: string}
        rotation_hint_days: {type: integer}
        created_by: {type: integer}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
    DeletedSecretEntry:
      type: object
      description: One row of a project's recycle bin (GET /projects/{id}/secrets/deleted) -- a snake_case DTO, not the raw Secret model.
      properties:
        id: {type: integer}
        name: {type: string}
        type: {type: string}
        classification: {type: string}
        deleted_at: {type: string}
    Group:
      type: object
      description: A group (server/http/handlers/groups_handler.go's groupToAPIResponse).
      properties:
        id: {type: integer}
        name: {type: string}
        description: {type: string}
    UserSummary:
      type: object
      description: >-
        A user as returned in list/membership contexts (userToAPIResponse,
        server/http/handlers/users_handler.go). project_count/active_project_count
        are attached only by GET /api/v1/users (listUsers), not by
        GET /api/v1/groups/{id}/members.
      properties:
        id: {type: integer}
        username: {type: string}
        email: {type: string}
        display_name: {type: string}
        active: {type: boolean}
        account_state: {type: string}
        external_id: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        last_login_at: {type: string, format: date-time, nullable: true}
        deleted_at: {type: string, format: date-time, nullable: true}
        project_count: {type: integer}
        active_project_count: {type: integer}
    GroupRoleGrant:
      type: object
      description: One role a group holds, with its time-bound expiry if any (internal/core/storage.GroupRoleGrant).
      properties:
        id: {type: integer}
        name: {type: string}
        description: {type: string}
        expires_at: {type: string, format: date-time, nullable: true}
    RoleRef:
      type: object
      description: A minimal role reference (id, name only) -- GET /api/v1/users/{id}/roles's shape.
      properties:
        id: {type: integer}
        name: {type: string}
    Permission:
      type: object
      description: >-
        A permission. internal/storage/models.Permission carries no `json:` tags,
        so the wire keys are the bare (capitalized) Go field names, not the
        snake_case this file uses elsewhere -- documented as such rather than as
        an idealized snake_case shape it does not actually have. Single-word
        field names still decode correctly into a snake_case-tagged Go client:
        encoding/json's Unmarshal falls back to a case-insensitive match
        ("ID" ~ "id") when no exact tag match exists.
      properties:
        ID: {type: integer}
        Name: {type: string}
        Description: {type: string}
        Resource: {type: string}
        Action: {type: string}
    RoleWithPermissions:
      type: object
      description: >-
        A role with its assigned permissions (internal/core.RoleWithPermissions,
        embeds models.Role). Same untagged-model caveat as Permission above: wire
        keys are the bare Go field names (ID, Name, Description,
        BypassesPermissionChecks, Permissions), not snake_case.
      properties:
        ID: {type: integer}
        Name: {type: string}
        Description: {type: string}
        BypassesPermissionChecks: {type: boolean}
        Permissions: {type: array, items: {$ref: '#/components/schemas/Permission'}}
    Project:
      type: object
      description: >-
        A project. Handler-level snake_case wire type
        (server/http/handlers/catalog_wire.go's projectWire) --
        internal/storage/models.Project itself carries no `json:` tags and is
        never serialized directly.
      properties:
        id: {type: integer}
        name: {type: string}
        description: {type: string}
        require_mfa: {type: boolean}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        deleted_at: {type: string, format: date-time}
    Environment:
      type: object
      description: >-
        A project environment. Handler-level snake_case wire type
        (server/http/handlers/catalog_wire.go's environmentWire) --
        internal/storage/models.Environment itself carries no `json:` tags and
        is never serialized directly.
      properties:
        id: {type: integer}
        project_id: {type: integer}
        name: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        deleted_at: {type: string, format: date-time}
    EnvCloneResult:
      type: object
      description: >-
        The result of cloning one environment's secrets into another
        (internal/core.EnvCloneResult). Handler-level snake_case wire type
        (server/http/handlers/catalog_wire.go's envCloneResultWire) --
        internal/core.EnvCloneResult itself carries no `json:` tags and is
        never serialized directly.
      properties:
        source_env: {type: string}
        dest_env: {type: string}
        secrets_cloned: {type: integer}
        secrets_skipped: {type: integer}
        errors: {type: array, items: {type: string}}
    ProjectInvitation:
      type: object
      description: >-
        A project invitation (ADR-024, internal/storage/models.ProjectInvitation).
        No `json:` tags on the model -- wire keys are the bare Go field names
        (ID, ProjectID, Email, ...), not snake_case. As with Environment above,
        the multi-word field names here do not case-insensitively match a
        snake_case tag, so a generated client must use these exact property
        names.
      properties:
        ID: {type: integer}
        ProjectID: {type: integer}
        Email: {type: string}
        Role: {type: string}
        State: {type: string, enum: [pending, accepted, revoked, expired]}
        InvitedBy: {type: integer}
        InvitedByMachineIdentityID: {type: integer}
        SystemRole: {type: string}
        ExpiresAt: {type: string, format: date-time, nullable: true}
        CreatedAt: {type: string, format: date-time}
        AcceptedAt: {type: string, format: date-time, nullable: true}
        RevokedAt: {type: string, format: date-time, nullable: true}
    ProvisionSetupResult:
      type: object
      description: Outcome of provisioning and delivering a setup/accept link (ADR-028, internal/core.ProvisionSetupResult).
      properties:
        email: {type: string}
        channel: {type: string, enum: [smtp, out_of_band, log]}
        delivered: {type: boolean}
        link_for_admin: {type: string, description: "Set only in out-of-band mode."}
    RBACAuditLogEntry:
      type: object
      description: One RBAC audit trail entry (GET /api/v1/audit/rbac-logs).
      properties:
        id: {type: integer}
        action: {type: string}
        actor_user_id: {type: integer, nullable: true}
        target_user_id: {type: integer, nullable: true}
        group_id: {type: integer, nullable: true}
        role_id: {type: integer, nullable: true}
        permission_id: {type: integer, nullable: true}
        project_id: {type: integer, nullable: true}
        environment_id: {type: integer, nullable: true}
        details: {type: string}
        created_at: {type: string, format: date-time}
    PermissionMatrixRow:
      type: object
      description: >-
        One (user, role, permission, scope) tuple (internal/core.PermissionMatrixRow),
        GET /api/v1/rbac/permission-matrix.
      properties:
        user_id: {type: integer}
        username: {type: string}
        email: {type: string}
        role_id: {type: integer}
        role_name: {type: string}
        permission_name: {type: string}
        resource: {type: string}
        action: {type: string}
        scope: {type: string, enum: [global, project, environment]}
        project_id: {type: integer}
        project_name: {type: string}
        environment_id: {type: integer}
        environment_name: {type: string}
        expires_at: {type: string, format: date-time, nullable: true}
    BreakGlassActivation:
      type: object
      description: One emergency-access self-grant (ADR-108 PR 1 addition).
      properties:
        id: {type: integer, format: uint32}
        user_id: {type: integer, format: uint32}
        role_name: {type: string}
        justification: {type: string}
        state: {type: string, enum: [active, expired, revoked]}
        # Deliberately plain `string`, not `format: date-time`: the CLI passes
        # these straight through to the terminal, and a date-time format would
        # make oapi-codegen generate a time.Time field whose default String()
        # rendering would not byte-match the server's original RFC3339 text.
        expires_at: {type: string}
        created_at: {type: string}
    RotationPolicy:
      type: object
      description: >-
        A secret-rotation policy (ADR-108 PR 1 addition). Snake_case, matching this
        spec's usual convention and the model's real wire format: models.RotationPolicy
        (internal/storage/models/models.go) previously had no `json:` tags on most
        fields, so encoding/json's default marshaling emitted the bare Go field names
        verbatim (ID, Name, ProjectID, IntervalDays, ...) instead -- a PascalCase wire
        format silently decoded by any snake_case-tagged consumer (Go's
        case-insensitive JSON fallback matches "ID"~"id" but not
        "ProjectID"~"project_id", an extra underscore is not a case difference) as the
        zero value for every multi-word field. Fixed at the source (the model itself
        now carries these exact tags) rather than documented here as a PascalCase
        exception -- see the model's own doc comment for the full writeup, and
        docs/cli-split-inventory.md §7 PR 1's closure note for how this was found.
      properties:
        id: {type: integer, format: uint32}
        name: {type: string}
        description: {type: string}
        scope: {type: string, enum: [project, environment]}
        project_id: {type: integer, format: uint32, nullable: true}
        environment_id: {type: integer, format: uint32, nullable: true}
        interval_days: {type: integer}
        alert_days_before: {type: integer}
        notify_on_breach: {type: boolean}
        is_active: {type: boolean}
        created_by: {type: string}
    RotationPolicyEvaluation:
      type: object
      description: >-
        One policy-covered secret's rotation posture, as returned by
        GET /api/v1/rotation-policies/evaluate (ADR-108 PR 1 addition).
      properties:
        policy_name: {type: string}
        secret_id: {type: integer, format: uint32}
        secret_name: {type: string}
        project_id: {type: integer, format: uint32}
        days_overdue: {type: integer}
        is_overdue: {type: boolean}
        is_approaching: {type: boolean}
    DynamicSecretConfig:
      type: object
      description: A registered dynamic-secret target config (ADR-108 PR 1 addition).
      properties:
        id: {type: integer, format: uint32}
        name: {type: string}
        project_id: {type: integer, format: uint32}
        environment_id: {type: integer, format: uint32}
        backend_type: {type: string, enum: [postgres, mysql, mongodb, redis, aws-sts, gcp, azure, kubernetes]}
        default_ttl_seconds: {type: integer}
        max_ttl_seconds: {type: integer}
        classification: {type: string}
    DynamicSecretIssuedLease:
      type: object
      description: >-
        A freshly-issued dynamic-secret credential, shown once (ADR-108 PR 1 addition).
        username/password are populated for DB/cache backends; fields carries
        arbitrary key/value pairs for cloud-IAM backends (AWS STS, GCP, Azure,
        Kubernetes) instead.
      properties:
        lease_id: {type: string}
        username: {type: string}
        password: {type: string}
        fields:
          type: object
          additionalProperties: {type: string}
        expires_at: {type: string}
    DynamicSecretLease:
      type: object
      description: A dynamic-secret lease summary (ADR-108 PR 1 addition).
      properties:
        lease_id: {type: string}
        role_name: {type: string}
        status: {type: string, enum: [active, revoked, expired, revoke_failed]}
        issued_at: {type: string}
        expires_at: {type: string}
    RotationPlanSecret:
      type: object
      description: One secret entry in a rotation-plan wave (ADR-108 PR 1 addition, ADR-053).
      properties:
        secret_id: {type: integer, format: uint32}
        secret_name: {type: string}
        status: {type: string, enum: [overdue, due_soon]}
        days_overdue: {type: integer}
        risk_band: {type: string}
        auto_rotate: {type: boolean}
        reasons:
          type: array
          items: {type: string}
    RotationPlanWave:
      type: object
      description: A dependency-safe batch of secrets to rotate together (ADR-053).
      properties:
        index: {type: integer}
        secrets:
          type: array
          items: {$ref: '#/components/schemas/RotationPlanSecret'}
    RotationPlan:
      type: object
      description: A project's automated rotation plan (ADR-108 PR 1 addition, ADR-053).
      properties:
        project_id: {type: integer, format: uint32}
        total_secrets: {type: integer}
        overdue_count: {type: integer}
        due_soon_count: {type: integer}
        waves:
          type: array
          items: {$ref: '#/components/schemas/RotationPlanWave'}
    DeploymentRotationPlan:
      type: object
      description: >-
        The install-wide roll-up of every project's rotation plan (ADR-108 PR 1
        addition, ADR-053) -- a roll-up of per-project plans, most pressing first.
      properties:
        projects_scanned: {type: integer}
        projects_with_work: {type: integer}
        total_secrets: {type: integer}
        overdue_count: {type: integer}
        due_soon_count: {type: integer}
        projects:
          type: array
          items: {$ref: '#/components/schemas/RotationPlan'}
    RotationOrderStep:
      type: object
      description: One secret in a project's safe rotation sequence (ADR-052).
      properties:
        secret_id: {type: integer, format: uint32}
        secret_name: {type: string}
    RotationOrder:
      type: object
      description: >-
        A safe rotation sequence for a project's secret dependency graph
        (ADR-108 PR 1 addition, ADR-052): each secret is listed before anything
        that depends on it.
      properties:
        project_id: {type: integer, format: uint32}
        order:
          type: array
          items: {$ref: '#/components/schemas/RotationOrderStep'}
